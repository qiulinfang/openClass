# Cursor 全局规则

## 自动总结规则

**重要：不要自动执行 `/sum` 或 `/md` 命令，除非满足以下所有条件：**

### 必须满足的条件

1. **问题已完全解决** ✅
   - 所有代码修改已完成并保存
   - 没有未完成的 TODO 或待办任务
   - 没有 linter 错误、编译错误或运行时错误
   - 用户明确表示问题已解决，或没有提出新的问题

2. **问题值得总结** 📝
   - 技术复杂度高（涉及复杂技术、架构设计或算法）
   - 有学习价值（包含可复用经验、最佳实践或设计模式）
   - 容易再次遇到（类似问题可能在其他地方出现）
   - 涉及多个文件/模块（需要记录影响范围）
   - 有特殊技巧（使用了特殊的技巧、工具或方法）
   - **或用户明确要求总结**

### 不应该总结的情况 ❌

- ❌ 简单问题（只是简单的代码修改、格式调整或拼写错误）
- ❌ 问题未解决（问题还在讨论中，代码还未修改，或修改未完成）
- ❌ 信息查询（只是查询信息、解释概念，没有实际代码修改）
- ❌ 日常对话（普通的问答、闲聊或非技术性对话）
- ❌ 已有相同总结（相同问题已经总结过）

### 执行流程

1. **判断问题状态**：检查问题是否已解决，是否值得总结
2. **询问用户**：如果满足条件，提示用户："问题已解决，是否需要生成总结文档？"
3. **等待确认**：等待用户确认后再执行 `/sum` 命令
4. **不满足条件**：如果不满足条件，不执行 `/sum` 命令，不主动提示

### 特殊情况

- **多轮对话**：只总结已完全解决的问题，未解决的问题不总结
- **部分解决**：如果问题只解决了一部分，不总结，等待完全解决
- **用户主动要求**：如果用户明确要求总结，可以执行，但需提醒问题可能还未完全解决

### 核心原则

- **保守原则**：不确定时，不执行总结，等待用户明确要求
- **用户优先**：如果用户明确要求总结，即使不完全满足条件也可以执行
- **避免重复**：执行前检查 `.specstory/sum/` 目录，避免重复总结
- **质量优先**：只总结有价值的问题，不要为了总结而总结

---

详细规则请参考：`.cursor/rules/auto-summary.md`













