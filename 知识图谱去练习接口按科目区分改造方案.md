# 知识图谱去练习接口按科目区分改造方案

## 一、问题分析

### 1.1 当前实现情况

#### Android端实现
- **文件位置**：`app/src/main/java/com/cosinetech/imates/ui/activities/FindExerciseActivity.java`
- **当前接口**：第165行使用 `ApiUrl.URL_QUERY_SIMILAR_EXERCISE_BY_KNOWLEDGE`
- **接口定义**：`ApiUrl.java` 第105行定义为 `/biologyTopicKnowledge/knowledgeTopicAndAck`
- **区分方式**：通过请求参数中的 `type` 字段区分学科（"math" 或 "biology"）

```java
// FindExerciseActivity.java 第165-177行
String url = ApiUrl.URL_QUERY_SIMILAR_EXERCISE_BY_KNOWLEDGE;  // 固定使用同一个接口
String subjectName;
if(mSubject == Subject.SUBJECT_BIOLOGY) {
    subjectName = "biology";
} else if(mSubject == Subject.SUBJECT_MATH){
    subjectName = "math";
}
mFindSimilarQuestionRequest.setType(subjectName);  // 通过参数区分
```

#### Vue Web端实现
- **文件位置**：`imates-web/src/services/api-service.ts`
- **当前接口**：使用 `API_ENDPOINTS.EXERCISES.SIMILAR_BY_KNOWLEDGE`
- **接口定义**：`api-endpoints.ts` 第54行定义为 `/biologyTopicKnowledge/knowledgeTopicAndAck`
- **区分方式**：同样通过请求参数中的 `type` 字段区分学科

### 1.2 问题总结

1. **接口命名不一致**：
   - 接口路径包含 "biology"，但实际同时用于数学和生物两个学科
   - 命名暗示只用于生物，但代码中同时支持数学

2. **接口设计不一致**：
   - 查询习题列表接口按学科分开：
     - `/permission/selectExercises/biology`
     - `/permission/selectExercises/math`
   - 但根据知识点查题的接口使用同一个路径：
     - `/biologyTopicKnowledge/knowledgeTopicAndAck`（通过参数区分）

3. **可维护性差**：
   - 如果后端需要为不同学科提供不同的接口逻辑，当前设计无法支持
   - 接口路径名称不清晰，容易引起误解

## 二、改造方案

### 2.1 方案概述

根据科目调用不同的接口路径，与查询习题列表接口的设计保持一致。

### 2.2 假设后端接口设计

**方案A（推荐）**：后端提供按学科分开的接口路径
- 数学：`/mathTopicKnowledge/knowledgeTopicAndAck`
- 生物：`/biologyTopicKnowledge/knowledgeTopicAndAck`

**方案B**：后端提供通用接口路径
- 通用：`/topicKnowledge/knowledgeTopicAndAck`
- 通过路径参数区分：`/topicKnowledge/knowledgeTopicAndAck/math` 和 `/topicKnowledge/knowledgeTopicAndAck/biology`

**方案C**：后端保持当前接口，但前端按科目区分（如果后端确实只支持一个接口）
- 如果后端确实只支持 `/biologyTopicKnowledge/knowledgeTopicAndAck`，且通过参数区分
- 前端代码可以保持不变，但需要添加注释说明

### 2.3 推荐方案（方案A）

假设后端提供按学科分开的接口，前端代码改造如下：

#### 2.3.1 Android端改造

**文件1：`app/src/main/java/com/cosinetech/imates/coreapiservice/ApiUrl.java`**

```java
// 在 ApiUrl 类中添加新的接口常量
// 根据知识点查题 - 数学
public static String URL_QUERY_SIMILAR_EXERCISE_BY_KNOWLEDGE_MATH;

// 根据知识点查题 - 生物
public static String URL_QUERY_SIMILAR_EXERCISE_BY_KNOWLEDGE_BIOLOGY;
```

在 `updateAllUrl()` 方法中初始化：

```java
// 根据知识点查题 - 数学
URL_QUERY_SIMILAR_EXERCISE_BY_KNOWLEDGE_MATH = baseUrl + "/mathTopicKnowledge/knowledgeTopicAndAck";

// 根据知识点查题 - 生物
URL_QUERY_SIMILAR_EXERCISE_BY_KNOWLEDGE_BIOLOGY = baseUrl + "/biologyTopicKnowledge/knowledgeTopicAndAck";
```

**文件2：`app/src/main/java/com/cosinetech/imates/ui/activities/FindExerciseActivity.java`**

修改 `findSimilarKnowledgeQuestion()` 方法：

```java
private void findSimilarKnowledgeQuestion() {
    StringBuilder ids = new StringBuilder();
    for (Question qq: mQuestionsInFavor) {
        ids.append(qq.bmNo).append(",");
    }

    // 根据科目选择不同的接口
    String url;
    if(mSubject == Subject.SUBJECT_BIOLOGY) {
        url = ApiUrl.URL_QUERY_SIMILAR_EXERCISE_BY_KNOWLEDGE_BIOLOGY;
    } else if(mSubject == Subject.SUBJECT_MATH){
        url = ApiUrl.URL_QUERY_SIMILAR_EXERCISE_BY_KNOWLEDGE_MATH;
    } else {
        return;
    }

    String subjectName;
    if(mSubject == Subject.SUBJECT_BIOLOGY) {
        subjectName = "biology";
    } else if(mSubject == Subject.SUBJECT_MATH){
        subjectName = "math";
    } else {
        return;
    }

    mFindSimilarQuestionRequest.setKnowledgeNo(mKnowledgeList);
    mFindSimilarQuestionRequest.setExercisesId(ids.toString());
    mFindSimilarQuestionRequest.setType(subjectName);
    mFindSimilarQuestionRequest.setPageSize(QUESTION_PAGE_SIZE);
    mFindSimilarQuestionRequest.setCurrentPage(mFindSimilarQuestionRequest.getCurrentPage() + 1);

    ApiGateWayService.querySimilarExerciseList(mFindSimilarQuestionRequest, url, mUserInfoViewModel.token.getValue(), mSimilarQuestionsCallback);
}
```

#### 2.3.2 Vue Web端改造

**文件1：`imates-web/src/services/api-endpoints.ts`**

```typescript
// 习题相关 - 与Android原生ApiUrl.java的配置，保持与原生接口一致
EXERCISES: {
  ADD: '/permission/exercises',
  LIST_BIOLOGY: '/permission/selectExercises/biology',
  LIST_MATH: '/permission/selectExercises/math',
  DELETE_BASE: '/permission/deleteExercises',
  SIMILAR: '/permission/topicAndAck',
  // 根据知识点查题 - 按学科分开
  SIMILAR_BY_KNOWLEDGE_MATH: '/mathTopicKnowledge/knowledgeTopicAndAck',
  SIMILAR_BY_KNOWLEDGE_BIOLOGY: '/biologyTopicKnowledge/knowledgeTopicAndAck',
  // 兼容旧版本（可以保留一段时间后删除）
  // SIMILAR_BY_KNOWLEDGE: '/biologyTopicKnowledge/knowledgeTopicAndAck',
  QUERY_KNOWLEDGE_BY_CHAPTER: '/knowledge',
},
```

**文件2：`imates-web/src/services/api-service.ts`**

修改 `findSimilarQuestionsByKnowledge()` 方法：

```typescript
public async findSimilarQuestionsByKnowledge(
  request: FindSimilarQuestionByKnowledgeRequest
): Promise<{
  questions: ExerciseItem[]
  totalCount: number
  pageSize: number
  currentPage: number
}> {
  try {
    // 根据科目选择不同的接口
    const url = request.type === 'math' 
      ? getApiUrl(API_ENDPOINTS.EXERCISES.SIMILAR_BY_KNOWLEDGE_MATH)
      : getApiUrl(API_ENDPOINTS.EXERCISES.SIMILAR_BY_KNOWLEDGE_BIOLOGY)
    
    // ... 其余代码保持不变
  }
}
```

## 三、改造步骤

### 3.1 验证后端接口

**重要**：在开始改造前，需要先验证后端是否提供按学科分开的接口：

1. **如果后端已提供**：
   - 数学：`/mathTopicKnowledge/knowledgeTopicAndAck`
   - 生物：`/biologyTopicKnowledge/knowledgeTopicAndAck`
   - 则按照方案A进行改造

2. **如果后端未提供**：
   - 需要先与后端协商，提供按学科分开的接口
   - 或者确认后端是否支持通过路径参数区分（方案B）

3. **如果后端只支持一个接口**：
   - 保持当前实现，但添加注释说明
   - 或者建议后端提供更通用的接口路径

### 3.2 改造顺序

1. **第一步**：验证后端接口支持情况
2. **第二步**：修改接口定义（`ApiUrl.java` 和 `api-endpoints.ts`）
3. **第三步**：修改接口调用逻辑（`FindExerciseActivity.java` 和 `api-service.ts`）
4. **第四步**：测试验证
5. **第五步**：更新文档

## 四、风险评估

### 4.1 兼容性风险

- 如果后端接口路径发生变化，需要确保Android和Vue Web端同步更新
- 建议保留旧的接口路径一段时间，通过配置开关控制使用新接口还是旧接口

### 4.2 测试风险

- 需要充分测试数学和生物两个学科的接口调用
- 需要验证接口调用的正确性和错误处理

## 五、总结

### 5.1 改造目标

根据科目调用不同的接口路径，与查询习题列表接口的设计保持一致，提高代码的可维护性和可读性。

### 5.2 关键决策点

1. **后端接口支持情况**：需要先验证后端是否提供按学科分开的接口
2. **接口路径设计**：确认使用 `/mathTopicKnowledge/knowledgeTopicAndAck` 还是其他路径
3. **兼容性处理**：是否需要保留旧接口路径的兼容性

### 5.3 下一步行动

1. **验证后端接口**：确认后端是否提供按学科分开的接口
2. **与后端协商**：如果需要，与后端协商接口路径设计
3. **实施改造**：根据后端接口支持情况，按照相应方案进行改造
4. **测试验证**：充分测试改造后的功能
5. **更新文档**：更新相关技术文档

