# 微课链接拼接逻辑分析

## 📋 概述

微课链接的拼接逻辑在不同平台和组件中有不同的实现方式。本文档详细分析了各个位置的链接拼接逻辑。

---

## 1. Web端实现（动态拼接）

### 1.1 主要实现位置

**文件**: `imates-web/src/views/ExerciseSolveView.vue`

**函数**: `handleOpenMiniClass(question: ExerciseItem)`

### 1.2 拼接逻辑详解

```typescript
// 第1步：获取题目编号（bmNo）
const bmNo = (question.bmNo || question.id || '').trim()
if (!bmNo) {
  showMessage('题目编号缺失，无法打开微课', 'warning')
  return
}

// 第2步：规范化学科前缀
const subjectRaw = (question.subject || userStore.subject || 'SUBJECT_MATH').toString().toUpperCase()
let subjectPrefix = 'math'  // 默认值

// 学科映射规则
if (subjectRaw.includes('BIOLOGY')) subjectPrefix = 'biology'
else if (subjectRaw.includes('MATH')) subjectPrefix = 'math'
else if (subjectRaw.includes('CHEMISTRY')) subjectPrefix = 'chemistry'
else if (subjectRaw.includes('PHYSICS')) subjectPrefix = 'physics'
else if (subjectRaw.includes('CHINESE')) subjectPrefix = 'chinese'
else if (subjectRaw.includes('ENGLISH')) subjectPrefix = 'english'

// 第3步：拼接完整URL
const classUrl = `https://www.imates.com.cn:9099/wk/${subjectPrefix}/${bmNo}/${bmNo}.html`
```

### 1.3 URL格式

**格式**: `https://www.imates.com.cn:9099/wk/{subjectPrefix}/{bmNo}/{bmNo}.html`

**示例**:
- 数学题目（bmNo=12345）: `https://www.imates.com.cn:9099/wk/math/12345/12345.html`
- 生物题目（bmNo=67890）: `https://www.imates.com.cn:9099/wk/biology/67890/67890.html`
- 化学题目（bmNo=11111）: `https://www.imates.com.cn:9099/wk/chemistry/11111/11111.html`

### 1.4 学科映射表

| 原始学科值 | 匹配规则 | 映射后的前缀 |
|-----------|---------|------------|
| `SUBJECT_BIOLOGY` | `includes('BIOLOGY')` | `biology` |
| `SUBJECT_MATH` | `includes('MATH')` | `math` |
| `SUBJECT_CHEMISTRY` | `includes('CHEMISTRY')` | `chemistry` |
| `SUBJECT_PHYSICS` | `includes('PHYSICS')` | `physics` |
| `SUBJECT_CHINESE` | `includes('CHINESE')` | `chinese` |
| `SUBJECT_ENGLISH` | `includes('ENGLISH')` | `english` |
| 其他/未定义 | 默认值 | `math` |

### 1.5 URL可用性检测

在打开微课前，会先检测URL是否可用：

```typescript
const checkUrlExists = (url: string): Promise<boolean> => {
  return new Promise((resolve) => {
    try {
      const img = new Image()
      img.onload = () => resolve(true)  // 200 时会触发 onload
      img.onerror = () => resolve(false) // 404/网络错触发 onerror
      // 追加时间戳避免缓存
      img.src = `${url}${url.includes('?') ? '&' : '?'}_ts=${Date.now()}`
    } catch {
      resolve(false)
    }
  })
}
```

**检测逻辑**:
1. 使用 `Image` 对象加载URL（规避CORS限制）
2. `onload` 触发 → URL可用（返回200）
3. `onerror` 触发 → URL不可用（404或网络错误）
4. 添加时间戳参数避免缓存影响检测结果

**处理结果**:
- ✅ URL可用 → 正常打开微课对话框
- ❌ URL不可用 → 打开对话框但不传URL，显示"暂无微课内容"

---

## 2. Web端实现（硬编码 - 旧版本）

### 2.1 实现位置

**文件**: `imates-web/src/components/QuestionList.vue`

**函数**: `openMiniClass(question: ExerciseItem)`

### 2.2 硬编码逻辑

```typescript
const openMiniClass = async (question: ExerciseItem) => {
  try {
    // 使用硬编码的微课URL
    const classUrl = 'https://www.imates.com.cn:9099/demo/demo1.html'
    
    if (!classUrl || classUrl.trim() === '') {
      showMessage('该题目暂无微课', 'warning')
      return
    }
    
    // 优先通过事件通知父组件（ExerciseSolveView）打开微课
    emit('openMiniClass', question)
  } catch {
    showMessage('打开微课失败', 'error')
  }
}
```

**特点**:
- ❌ 所有题目使用同一个URL
- ❌ 无法根据题目动态加载对应的微课
- ⚠️ 这是旧版本的实现，可能已被 `ExerciseSolveView.vue` 的动态拼接逻辑替代

---

## 3. Android端实现（动态拼接）

### 3.1 实现位置

**文件**: `app/src/main/java/com/cosinetech/imates/ui/activities/ExerciseSolveActivity.java`

**方法**: `getMiniClassUrl()`

### 3.2 动态拼接逻辑

```java
private String getMiniClassUrl() {
    // 第1步：获取学科前缀（使用SubjectUtils工具类）
    String prefix = SubjectUtils.getSubjectNormalizedName(subject);
    
    try {
        // 第2步：获取当前题目的编号（bmNo）
        if(mCurrentQuestionIndex >= 0 && mCurrentQuestionIndex < mQuestions.size()){
            String id = mQuestions.get(mCurrentQuestionIndex).bmNo;
            
            // 第3步：拼接完整URL
            return "https://www.imates.com.cn:9099/wk/" + prefix + "/" + id + "/" + id + ".html";
        } else {
            return "";
        }
    } catch (Exception e) {
        return "";
    }
}
```

### 3.3 学科映射工具类

**文件**: `app/src/main/java/com/cosinetech/imates/data/models/SubjectUtils.java`

**方法**: `getSubjectNormalizedName(Subject subject)`

```java
public static String getSubjectNormalizedName(Subject subject) {
    return switch (subject) {
        case SUBJECT_BIOLOGY -> "biology";
        case SUBJECT_MATH -> "math";
        case SUBJECT_CHINESE -> "chinese";
        case SUBJECT_ENGLISH -> "english";
        case SUBJECT_CHEMISTRY -> "chemistry";
        case SUBJECT_PHYSICS -> "physics";
        case SUBJECT_ALL -> "all";
    };
}
```

### 3.4 URL格式

**格式**: `https://www.imates.com.cn:9099/wk/{prefix}/{id}/{id}.html`

**参数说明**:
- `prefix`: 学科前缀（通过 `SubjectUtils.getSubjectNormalizedName()` 获取）
- `id`: 题目编号（从 `mQuestions.get(mCurrentQuestionIndex).bmNo` 获取）

**示例**:
- 数学题目（bmNo=12345）: `https://www.imates.com.cn:9099/wk/math/12345/12345.html`
- 生物题目（bmNo=67890）: `https://www.imates.com.cn:9099/wk/biology/67890/67890.html`

### 3.5 特点

- ✅ **动态拼接**: 根据当前题目和学科动态生成URL
- ✅ **使用工具类**: 通过 `SubjectUtils` 统一管理学科映射
- ✅ **异常处理**: 包含 try-catch 异常处理
- ⚠️ **边界检查**: 检查题目索引有效性
- ⚠️ **空值处理**: 如果索引无效或异常，返回空字符串

---

## 4. URL结构分析

### 4.1 基础URL

**基础域名**: `https://www.imates.com.cn:9099`

**端口**: `9099`

### 4.2 路径结构

#### 动态拼接路径（Web端新版本）
```
/wk/{subjectPrefix}/{bmNo}/{bmNo}.html
```

**路径说明**:
- `/wk/` - 微课资源根路径
- `{subjectPrefix}` - 学科前缀（math/biology/chemistry/physics/chinese/english）
- `{bmNo}` - 题目编号（bmNo或id）
- `{bmNo}.html` - HTML文件名（与题目编号相同）

#### 硬编码路径（旧版本）
```
/demo/demo1.html
```

**路径说明**:
- `/demo/` - 演示资源路径
- `demo1.html` - 固定的演示文件

### 4.3 完整URL示例对比

| 实现方式 | 题目编号 | 学科 | 完整URL |
|---------|---------|------|---------|
| **动态拼接** | `12345` | `SUBJECT_MATH` | `https://www.imates.com.cn:9099/wk/math/12345/12345.html` |
| **动态拼接** | `67890` | `SUBJECT_BIOLOGY` | `https://www.imates.com.cn:9099/wk/biology/67890/67890.html` |
| **硬编码** | 任意 | 任意 | `https://www.imates.com.cn:9099/demo/demo1.html` |

---

## 5. 数据来源分析

### 5.1 题目编号（bmNo）来源

**优先级顺序**:
1. `question.bmNo` - 题目的业务编号（优先）
2. `question.id` - 题目的ID（备选）
3. 空字符串（如果都不存在）

**验证**:
- 如果 `bmNo` 为空，会显示警告："题目编号缺失，无法打开微课"

### 5.2 学科（subject）来源

**优先级顺序**:
1. `question.subject` - 题目对象中的学科字段（优先）
2. `userStore.subject` - 用户Store中的学科（备选）
3. `'SUBJECT_MATH'` - 默认值（如果都不存在）

**处理**:
- 转换为大写字符串：`.toString().toUpperCase()`
- 使用 `includes()` 方法匹配学科关键词

---

## 6. 调用流程

### 6.1 Web端调用流程

```
用户点击微课按钮
    ↓
QuestionList.vue: openMiniClass()
    ↓
emit('openMiniClass', question)
    ↓
ExerciseSolveView.vue: handleOpenMiniClass(question)
    ↓
1. 提取 bmNo（question.bmNo || question.id）
2. 提取 subject（question.subject || userStore.subject）
3. 映射学科前缀（BIOLOGY → biology, MATH → math, ...）
4. 拼接URL: https://www.imates.com.cn:9099/wk/{subjectPrefix}/{bmNo}/{bmNo}.html
    ↓
checkUrlExists(classUrl) - 检测URL可用性
    ↓
URL可用?
    ├─ 是 → uiStore.openMiniClassDialog(classUrl, questionTitle)
    └─ 否 → uiStore.openMiniClassDialog('', questionTitle) + 显示"该题目暂无微课"
```

### 6.2 Android端调用流程

```
用户点击微课按钮
    ↓
ExerciseSolveActivity: findViewById(R.id.btn_mini_class).setOnClickListener()
    ↓
getMiniClassUrl() - 动态拼接URL
    ├─ 1. SubjectUtils.getSubjectNormalizedName(subject) → 获取学科前缀
    ├─ 2. mQuestions.get(mCurrentQuestionIndex).bmNo → 获取题目编号
    └─ 3. 拼接: https://www.imates.com.cn:9099/wk/{prefix}/{id}/{id}.html
    ↓
验证URL不为空
    ↓
Intent intent = new Intent(this, MiniClassActivity.class)
intent.putExtra(MiniClassActivity.KEY_MINI_CLASS_URL, url)
    ↓
startActivity(intent)
    ↓
MiniClassActivity 接收URL并加载到WebView
```

---

## 7. 问题与改进建议

### 7.1 当前问题

#### Web端
- ✅ **已改进**: `ExerciseSolveView.vue` 使用动态拼接逻辑
- ⚠️ **待清理**: `QuestionList.vue` 仍使用硬编码（可能已废弃）

#### Android端
- ✅ **已实现**: 使用动态拼接逻辑，根据题目和学科生成URL
- ✅ **工具类支持**: 使用 `SubjectUtils` 统一管理学科映射
- ⚠️ **边界处理**: 需要确保题目索引有效，否则返回空字符串

### 7.2 改进建议

#### 1. Android端增强错误处理

**当前问题**:
- 如果 `bmNo` 为空或无效，返回空字符串，但用户可能不知道原因
- 没有URL可用性检测

**建议改进**:
```java
private String getMiniClassUrl() {
    String prefix = SubjectUtils.getSubjectNormalizedName(subject);
    try {
        if(mCurrentQuestionIndex >= 0 && mCurrentQuestionIndex < mQuestions.size()){
            Question question = mQuestions.get(mCurrentQuestionIndex);
            String id = question.bmNo;
            
            // 增强：检查 bmNo 是否有效
            if (id == null || id.trim().isEmpty()) {
                // 可以尝试使用 id 字段作为备选
                id = question.id;
                if (id == null || id.trim().isEmpty()) {
                    Log.w(TAG, "题目编号缺失，无法生成微课URL");
                    return "";
                }
            }
            
            return "https://www.imates.com.cn:9099/wk/" + prefix + "/" + id + "/" + id + ".html";
        } else {
            Log.w(TAG, "题目索引无效: " + mCurrentQuestionIndex);
            return "";
        }
    } catch (Exception e) {
        Log.e(TAG, "生成微课URL失败", e);
        return "";
    }
}
```

#### 2. 统一URL拼接逻辑

**建议**:
- 创建统一的URL构建工具类/函数
- Web端和Android端共享相同的拼接逻辑
- 便于维护和修改

#### 3. 添加URL可用性检测（Android端）

**建议**:
- Android端也可以添加URL可用性检测
- 避免打开不存在的微课页面
- 提升用户体验

#### 4. 清理废弃代码

**建议**:
- 检查 `QuestionList.vue` 中的硬编码逻辑是否仍在使用
- 如果已废弃，删除相关代码
- 统一使用 `ExerciseSolveView.vue` 的动态拼接逻辑

---

## 8. 相关文件位置

### Web端
- ✅ `imates-web/src/views/ExerciseSolveView.vue:380-436` - **动态拼接逻辑（主要实现）**
- ⚠️ `imates-web/src/components/QuestionList.vue:1094-1109` - 硬编码逻辑（可能已废弃）
- 📄 `imates-web/src/components/MiniClass.vue` - 微课播放组件
- 📄 `imates-web/src/stores/uiStore.ts` - UI状态管理（微课对话框状态）

### Android端
- ✅ `app/src/main/java/com/cosinetech/imates/ui/activities/ExerciseSolveActivity.java:367-379` - **动态拼接逻辑（已实现）**
- 📄 `app/src/main/java/com/cosinetech/imates/data/models/SubjectUtils.java:31-41` - 学科映射工具类
- 📄 `app/src/main/java/com/cosinetech/imates/coreapiservice/Question.java` - 题目数据模型（包含 bmNo 字段）

### 文档
- 📄 `微课URL来源分析.md` - 微课URL来源分析文档
- 📄 `imates-web/docs-prd/src/components/MiniClass.vue.md` - MiniClass组件文档

---

## 9. 总结

### 9.1 当前状态

| 平台 | 实现方式 | 状态 | 备注 |
|------|---------|------|------|
| **Web端（ExerciseSolveView）** | 动态拼接 | ✅ 已实现 | 根据学科和题目编号动态拼接URL，包含URL可用性检测 |
| **Web端（QuestionList）** | 硬编码 | ⚠️ 待清理 | 可能已废弃，建议统一使用ExerciseSolveView的逻辑 |
| **Android端** | 动态拼接 | ✅ 已实现 | 根据学科和题目编号动态拼接URL，使用SubjectUtils工具类 |

### 9.2 核心拼接公式

**动态拼接公式**:
```
https://www.imates.com.cn:9099/wk/{subjectPrefix}/{bmNo}/{bmNo}.html
```

**参数说明**:
- `subjectPrefix`: 学科前缀（math/biology/chemistry/physics/chinese/english）
- `bmNo`: 题目编号（从 question.bmNo 或 question.id 获取）

### 9.3 关键逻辑对比

#### Web端（ExerciseSolveView.vue）
1. **题目编号提取**: `question.bmNo || question.id`
2. **学科映射**: 通过 `includes()` 匹配学科关键词（字符串匹配）
3. **URL检测**: 使用 `Image` 对象检测URL可用性（规避CORS）
4. **错误处理**: URL不可用时显示"暂无微课内容"

#### Android端（ExerciseSolveActivity.java）
1. **题目编号提取**: `mQuestions.get(mCurrentQuestionIndex).bmNo`
2. **学科映射**: 使用 `SubjectUtils.getSubjectNormalizedName()` 工具类（枚举switch）
3. **URL检测**: ❌ 未实现URL可用性检测
4. **错误处理**: 异常时返回空字符串，需要调用方处理

### 9.4 实现差异

| 特性 | Web端 | Android端 |
|------|-------|-----------|
| **学科映射方式** | 字符串 `includes()` 匹配 | 枚举 `switch` 匹配（更规范） |
| **工具类支持** | ❌ 无统一工具类 | ✅ 使用 `SubjectUtils` |
| **URL可用性检测** | ✅ 有检测 | ❌ 无检测 |
| **错误提示** | ✅ 有用户提示 | ⚠️ 仅日志记录 |
| **备选字段** | ✅ 支持 `id` 作为备选 | ❌ 仅使用 `bmNo` |

---

## 10. 统一拼接公式总结

### 10.1 核心拼接公式

**统一格式**:
```
https://www.imates.com.cn:9099/wk/{subjectPrefix}/{bmNo}/{bmNo}.html
```

### 10.2 参数获取方式对比

| 参数 | Web端 | Android端 |
|------|-------|-----------|
| **subjectPrefix** | 字符串匹配（`includes()`） | 枚举匹配（`SubjectUtils`） |
| **bmNo** | `question.bmNo \|\| question.id` | `mQuestions[index].bmNo` |
| **备选方案** | ✅ 支持 `id` 字段 | ❌ 仅使用 `bmNo` |

### 10.3 学科映射对比

| 学科枚举值 | Web端映射逻辑 | Android端映射逻辑 | 结果 |
|-----------|-------------|-----------------|------|
| `SUBJECT_BIOLOGY` | `subjectRaw.includes('BIOLOGY')` | `switch case SUBJECT_BIOLOGY` | `biology` |
| `SUBJECT_MATH` | `subjectRaw.includes('MATH')` | `switch case SUBJECT_MATH` | `math` |
| `SUBJECT_CHEMISTRY` | `subjectRaw.includes('CHEMISTRY')` | `switch case SUBJECT_CHEMISTRY` | `chemistry` |
| `SUBJECT_PHYSICS` | `subjectRaw.includes('PHYSICS')` | `switch case SUBJECT_PHYSICS` | `physics` |
| `SUBJECT_CHINESE` | `subjectRaw.includes('CHINESE')` | `switch case SUBJECT_CHINESE` | `chinese` |
| `SUBJECT_ENGLISH` | `subjectRaw.includes('ENGLISH')` | `switch case SUBJECT_ENGLISH` | `english` |
| 其他/未定义 | 默认 `math` | 默认 `math`（通过default case） | `math` |

### 10.4 完整示例

**场景**: 数学题目，bmNo = "12345"

**Web端生成过程**:
```typescript
// 1. 获取学科
const subjectRaw = "SUBJECT_MATH".toUpperCase()  // "SUBJECT_MATH"
// 2. 映射学科前缀
let subjectPrefix = 'math'  // subjectRaw.includes('MATH') → true
// 3. 获取题目编号
const bmNo = "12345"  // question.bmNo
// 4. 拼接URL
const url = `https://www.imates.com.cn:9099/wk/math/12345/12345.html`
```

**Android端生成过程**:
```java
// 1. 获取学科前缀
String prefix = SubjectUtils.getSubjectNormalizedName(SUBJECT_MATH)  // "math"
// 2. 获取题目编号
String id = mQuestions.get(index).bmNo  // "12345"
// 3. 拼接URL
String url = "https://www.imates.com.cn:9099/wk/" + prefix + "/" + id + "/" + id + ".html"
// 结果: "https://www.imates.com.cn:9099/wk/math/12345/12345.html"
```

**结果**: 两端生成的URL完全一致 ✅

---

*文档生成时间: 2024年*
*最后更新: 基于代码库分析，已补充Android端动态拼接逻辑*

