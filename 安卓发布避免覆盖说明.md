# Android 发布避免覆盖已安装应用说明

## 概述

为了避免在发布新版本时覆盖已安装的应用，项目已配置了 **Product Flavors（产品变体）**。不同的 flavor 使用不同的应用包名（applicationId），因此可以在同一设备上同时安装多个版本。

## 产品变体说明

### 1. Production（正式版）
- **包名**: `com.cosinetech.imates.bj101`
- **用途**: 正式发布版本
- **特点**: 标准包名，会覆盖已安装的正式版

### 2. Staging（测试版）
- **包名**: `com.cosinetech.imates.bj101.staging`
- **用途**: 测试版本
- **特点**: 不会覆盖正式版，可以同时安装

### 3. Dev（开发版）
- **包名**: `com.cosinetech.imates.bj101.dev`
- **用途**: 开发测试版本
- **特点**: 不会覆盖正式版和测试版，可以同时安装

## 构建命令

### 方式一：使用 Gradle 命令

#### 构建正式版 Release
```bash
./gradlew assembleProductionRelease
```

#### 构建测试版 Release
```bash
./gradlew assembleStagingRelease
```

#### 构建开发版 Release
```bash
./gradlew assembleDevRelease
```

#### 构建开发版 Debug（默认）
```bash
./gradlew assembleDevDebug
# 或简写
./gradlew assembleDebug
```

#### 构建所有变体的 Release 版本
```bash
./gradlew assembleRelease
```

### 方式二：在 Android Studio 中

**重要：首次使用或修改配置后，需要先同步 Gradle**

1. **同步 Gradle 配置**：
   - 点击菜单 **File → Sync Project with Gradle Files**
   - 或者点击工具栏的 **Sync Project with Gradle Files** 图标（🔄）
   - 或者使用快捷键：**Ctrl+Shift+O**（Windows/Linux）或 **Cmd+Shift+I**（Mac）
   - 等待同步完成（底部状态栏会显示进度）

2. 打开 **Build Variants** 窗口：
   - 菜单：**View → Tool Windows → Build Variants**
   - 或者点击左侧边栏的 **Build Variants** 标签

3. 在 **app** 模块的 **Active Build Variant** 下拉菜单中选择：
   - `productionDebug` - 正式版调试版
   - `productionRelease` - 正式发布版
   - `stagingDebug` - 测试版调试版
   - `stagingRelease` - 测试版发布版
   - `devDebug` - 开发版调试版（默认）
   - `devRelease` - 开发版发布版

4. 选择完成后，点击 **Run** 或 **Build → Make Project** 进行构建

**如果 Build Variants 窗口只显示 `devDebug` 和 `devRelease`**：
- 请确保已执行步骤 1 的 Gradle 同步
- 如果同步后仍不显示，尝试：
  - **File → Invalidate Caches... → Invalidate and Restart**
  - 或者在终端运行：`./gradlew clean` 然后重新同步

### 方式三：使用命令行参数（Windows）

```cmd
gradlew.bat assembleProductionRelease
gradlew.bat assembleStagingRelease
gradlew.bat assembleDevRelease
```

## 输出文件说明

构建完成后，APK 文件会按照以下格式命名：

```
app-{flavor}-{buildType}-{versionName}-{versionCode}.apk
```

**示例**：
- `app-production-release-1.0.77-77.apk` - 正式发布版
- `app-staging-release-1.0.77-staging-77.apk` - 测试版
- `app-dev-debug-1.0.77-dev-77.apk` - 开发调试版

APK 文件位置：
```
app/build/outputs/apk/{flavor}/{buildType}/
```

## 使用场景

### 场景 1：发布测试版而不覆盖正式版
```bash
./gradlew assembleStagingRelease
```
安装后，设备上会同时存在：
- 正式版：`com.cosinetech.imates.bj101`
- 测试版：`com.cosinetech.imates.bj101.staging`

### 场景 2：开发调试时不覆盖正式版
直接运行 Debug 构建（默认使用 `dev` flavor）：
```bash
./gradlew assembleDebug
```
或使用：
```bash
./gradlew assembleDevDebug
```

### 场景 3：发布正式版（会覆盖旧版本）
```bash
./gradlew assembleProductionRelease
```
⚠️ **注意**：这会覆盖设备上已安装的正式版应用

## 注意事项

1. **版本号管理**：发布正式版前，记得在 `app/build.gradle` 中更新 `versionCode` 和 `versionName`
2. **签名配置**：所有变体都使用相同的签名配置
3. **同时安装**：不同 flavor 的应用可以同时安装在同一设备上
4. **应用更新**：同一 flavor 的新版本会覆盖旧版本（如果 versionCode 更大）

## 修改版本号

在 `app/build.gradle` 中修改：

```groovy
defaultConfig {
    versionCode 77        // 版本代码，每次发布必须递增
    versionName "1.0.77"  // 版本名称，可以自定义格式
}
```

## 常见问题

### Q: 如何恢复到不使用 Product Flavors 的方式？
A: 如果不需要多个变体，可以移除 `productFlavors` 配置块，恢复原来的 `applicationIdSuffix` 方式。

### Q: Debug 构建默认使用哪个 flavor？
A: 默认使用 `dev` flavor，包名为 `com.cosinetech.imates.bj101.dev`

### Q: 如何查看当前构建的包名？
A: 构建完成后，APK 文件名中包含了 flavor 信息，也可以安装后在应用的设置中查看包名。

### Q: 可以添加更多的 flavor 吗？
A: 可以，在 `productFlavors` 块中添加新的 flavor 配置即可。

### Q: Build Variants 窗口只显示 devDebug 和 devRelease，没有其他选项？
A: 这是因为 Gradle 配置还没有同步。请按以下步骤操作：
1. **同步 Gradle**：菜单 → File → Sync Project with Gradle Files
2. 等待同步完成（查看底部状态栏）
3. 重新打开 Build Variants 窗口查看
4. 如果仍然不显示，尝试：
   - **清理项目**：菜单 → Build → Clean Project
   - **使缓存失效**：菜单 → File → Invalidate Caches... → Invalidate and Restart
   - 或者运行：`./gradlew clean` 然后在 Android Studio 中重新同步

### Q: 为什么会有 6 个 Build Variants（productionDebug, productionRelease, stagingDebug, stagingRelease, devDebug, devRelease）？
A: 这是因为每个 flavor（production、staging、dev）都可以和两种 buildType（debug、release）组合，所以总共有 3 × 2 = 6 个变体。
