# 会话和消息持久化存储分析

## 一、存储架构概述

系统采用**双存储机制**：
1. **会话信息**（元数据）：存储在 `localStorage`
2. **消息内容**（聊天历史）：存储在 `IndexedDB`（优先）或 `localStorage`（降级）

## 二、消息存储机制

### 2.1 存储服务

**位置**：`imates-web/src/services/chat-storage.ts`

**核心类**：`AsyncStorageService`（单例模式）

**存储策略**：
- **优先**：IndexedDB（使用 `localforage` 库）
- **降级**：localStorage（当 IndexedDB 不可用时）

### 2.2 消息存储键名格式

**实际存储键名**：
```
${userId}_chat_history_${questionId}
```

**questionId 格式**（不同场景）：
- AI通用对话：`ai-general-${sessionId}`
- AI题目对话：`ai-exercise-${questionId}`
- AI教材对话：`ai-textbook-${resourceId}`
- 教师通用对话：`teacher-general`（统一存储，所有会话共享）
- 教师题目对话：`teacher-exercise-${questionId}`

**示例**：
```
user123_chat_history_ai-general-session-abc123
user123_chat_history_ai-exercise-question-456
user123_chat_history_teacher-general
user123_chat_history_teacher-exercise-question-456
```

### 2.3 消息数据结构

**存储结构**：`ChatHistoryData`

```typescript
interface ChatHistoryData {
  questionId: string           // 存储键（用于标识）
  messages: ChatBubble[]       // 消息列表
  chatResponseTimes: number   // AI回复次数
  lastUpdated: number          // 最后更新时间戳
}
```

**消息结构**：`ChatBubble`

```typescript
interface ChatBubble {
  id: string                   // 消息ID
  content: string              // 消息内容
  sender: string               // 发送者（'user' | 'assistant'）
  type: string                 // 消息类型（'text' | 'image' | 'voice'）
  timestamp: number            // 时间戳
  messageId?: string           // 消息ID（可选）
  messageType?: string         // 消息类型（可选）
  isStreaming?: boolean        // 是否正在流式传输
  imageData?: {                // 图片数据
    filePath: string
    width: number
    height: number
    fileSize: number
    base64DataUrl: string      // base64图片数据（用于UI显示）
  }
  voiceData?: {                // 语音数据
    filePath: string
    duration: number
    fileSize: number
  }
  isError?: boolean            // 是否为错误消息
  canRetry?: boolean           // 是否可以重试
  retryCount?: number          // 重试次数
  originalMessage?: string     // 原始消息
  chatRecordData?: object      // 聊天记录数据
}
```

### 2.4 IndexedDB 配置

**数据库名**：`ExerciseSolveApp_${userId}`（使用用户ID作为前缀，实现账号隔离）

**存储表名**：`chat_history`

**配置代码**：
```typescript
function getUserLocalForage() {
  const userId = getCurrentUserIdOrDefault()
  return localforage.createInstance({
    driver: localforage.INDEXEDDB,
    name: `ExerciseSolveApp_${userId}`,
    version: 1.0,
    storeName: 'chat_history',
    description: `练习解题应用聊天记录存储 (用户: ${userId})`
  })
}
```

### 2.5 消息存储流程

**保存流程**：
```
1. 调用 asyncStorage.saveChatHistory(questionId, data)
   │
   ├─> 初始化 IndexedDB（如果未初始化）
   │
   ├─> 序列化数据（确保所有属性可存储）
   │
   ├─> 尝试保存到 IndexedDB
   │   └─> 成功：完成
   │   └─> 失败：降级到 localStorage
   │
   └─> 构建存储键：${userId}_chat_history_${questionId}
```

**加载流程**：
```
1. 调用 asyncStorage.loadChatHistory(questionId)
   │
   ├─> 初始化 IndexedDB（如果未初始化）
   │
   ├─> 尝试从 IndexedDB 加载
   │   └─> 成功：返回数据
   │   └─> 失败：降级到 localStorage
   │
   └─> 构建存储键：${userId}_chat_history_${questionId}
```

### 2.6 消息过滤规则

**保存时过滤**（不保存以下消息）：
- ❌ 错误消息（`isError: true`）
- ❌ 流式消息（`isStreaming: true`）
- ❌ 系统消息（`isSystemMessage: true`）
- ❌ 撤回消息（`isRecalled: true`）
- ❌ 无消息ID的消息（既无 `messageId` 也无 `id`）

## 三、会话存储机制

### 3.1 会话类型分类

系统中有**5种会话类型**，每种类型的存储方式不同：

| 会话类型 | 存储方式 | 存储键名格式 | 存储位置 |
|---------|---------|------------|---------|
| **教师通用会话** | 每个会话单独存储 | `${userId}_teacher-general-${sessionId}_session` | localStorage |
| **教师题目会话** | 所有会话统一存储 | `${userId}_teacher-exercise-sessions` | localStorage |
| **AI通用会话** | 所有会话统一存储 | `${userId}_ai-general-sessions` | localStorage |
| **AI题目会话** | 无会话列表 | - | - |
| **AI教材会话** | 无会话列表 | - | - |

### 3.2 教师通用会话存储

**存储方式**：每个会话单独存储在一条 localStorage 记录中

**存储键名**：
```
${userId}_teacher-general-${sessionId}_session
```

**示例**：
```
user123_teacher-general-teacher-abc123-1234567890_session
```

**数据结构**：
```typescript
interface TeacherSession {
  sessionId: string      // 会话ID
  sessionName: string    // 会话名称
  subject: string        // 科目：'biology' | 'math'
  createTime: number     // 创建时间戳
}
```

**存储操作**：
- **保存**：`localStorage.setItem(sessionKey, JSON.stringify(session))`
- **加载**：遍历 localStorage，查找所有以 `${userId}_teacher-general-` 开头且以 `_session` 结尾的键
- **删除**：`localStorage.removeItem(sessionKey)`

**代码位置**：`imates-web/src/stores/teacherGeneralChatStore.ts`

### 3.3 教师题目会话存储

**存储方式**：所有会话统一存储在一条 localStorage 记录中

**存储键名**：
```
${userId}_teacher-exercise-sessions
```

**数据结构**：
```typescript
// 存储结构：Record<string, TeacherExerciseSession>
{
  [sessionId: string]: TeacherExerciseSession
}

interface TeacherExerciseSession {
  sessionId: string
  questionId: string
  sessionName: string
  subject: 'biology' | 'math'
  createTime: number
  updateTime?: number
}
```

**存储操作**：
- **保存单个会话**：
  ```typescript
  const sessions = loadAllSessions()
  sessions[session.sessionId] = session
  saveAllSessions(sessions)
  ```
- **加载所有会话**：`localStorage.getItem(storageKey)`
- **删除单个会话**：
  ```typescript
  const sessions = loadAllSessions()
  delete sessions[sessionId]
  saveAllSessions(sessions)
  ```

**数据迁移**：支持从旧格式（每个会话单独存储）自动迁移到新格式

**代码位置**：`imates-web/src/stores/teacherExerciseChatStore.ts`

### 3.4 AI通用会话存储

**存储方式**：所有会话统一存储在一条 localStorage 记录中

**存储键名**：
```
${userId}_ai-general-sessions
```

**数据结构**：
```typescript
// 存储结构：AiGeneralSession[]
[
  {
    sessionId: string
    sessionName: string
    createTime: number
    updateTime: number
    msgCount: number
  },
  ...
]
```

**存储操作**：
- **保存会话列表**：`localStorage.setItem(key, JSON.stringify(sessions))`
- **加载会话列表**：`localStorage.getItem(key)`

**代码位置**：`imates-web/src/stores/aiGeneralChatStore.ts`

### 3.5 AI题目会话存储

**特点**：不需要会话列表，直接按题目ID存储消息

**消息存储键名**：
```
${userId}_chat_history_ai-exercise-${questionId}
```

**代码位置**：`imates-web/src/stores/aiExerciseChatStore.ts`

### 3.6 AI教材会话存储

**特点**：不需要会话列表，直接按资源ID存储消息

**消息存储键名**：
```
${userId}_chat_history_ai-textbook-${resourceId}
```

**代码位置**：`imates-web/src/stores/aiTextbookChatStore.ts`

## 四、存储键名汇总表

### 4.1 消息存储键名

| 场景 | 存储键名格式 | 示例 |
|-----|------------|------|
| AI通用对话 | `${userId}_chat_history_ai-general-${sessionId}` | `user123_chat_history_ai-general-session-abc` |
| AI题目对话 | `${userId}_chat_history_ai-exercise-${questionId}` | `user123_chat_history_ai-exercise-question-456` |
| AI教材对话 | `${userId}_chat_history_ai-textbook-${resourceId}` | `user123_chat_history_ai-textbook-resource-789` |
| 教师通用对话 | `${userId}_chat_history_teacher-general` | `user123_chat_history_teacher-general` |
| 教师题目对话 | `${userId}_chat_history_teacher-exercise-${questionId}` | `user123_chat_history_teacher-exercise-question-456` |

### 4.2 会话存储键名

| 场景 | 存储键名格式 | 示例 |
|-----|------------|------|
| 教师通用会话 | `${userId}_teacher-general-${sessionId}_session` | `user123_teacher-general-session-abc_session` |
| 教师题目会话 | `${userId}_teacher-exercise-sessions` | `user123_teacher-exercise-sessions` |
| AI通用会话 | `${userId}_ai-general-sessions` | `user123_ai-general-sessions` |
| AI题目会话 | 无会话列表 | - |
| AI教材会话 | 无会话列表 | - |

## 五、存储流程详解

### 5.1 教师通用会话保存流程

```
1. 用户发送消息
   │
   ├─> 创建/更新会话信息
   │   └─> 保存到 localStorage：${userId}_teacher-general-${sessionId}_session
   │
   ├─> 保存消息到 IndexedDB
   │   └─> 存储键：${userId}_chat_history_teacher-general-${sessionId}
   │
   └─> 防抖保存（1秒延迟）
```

### 5.2 教师题目会话保存流程

```
1. 用户发送消息
   │
   ├─> 创建/更新会话信息
   │   └─> 更新到统一存储：${userId}_teacher-exercise-sessions
   │       └─> 结构：Record<string, TeacherExerciseSession>
   │
   ├─> 保存消息到 IndexedDB
   │   └─> 存储键：${userId}_chat_history_teacher-exercise-${questionId}
   │
   └─> 防抖保存（1秒延迟）
```

### 5.3 AI通用会话保存流程

```
1. 用户发送消息
   │
   ├─> 更新会话信息（msgCount, updateTime）
   │   └─> 更新到统一存储：${userId}_ai-general-sessions
   │       └─> 结构：AiGeneralSession[]
   │
   ├─> 保存消息到 IndexedDB
   │   └─> 存储键：${userId}_chat_history_ai-general-${sessionId}
   │
   └─> 立即保存（无防抖）
```

## 六、账号隔离机制

### 6.1 用户ID前缀

**所有存储键名都包含用户ID前缀**，确保不同账号的数据隔离：

- ✅ 消息存储：`${userId}_chat_history_${questionId}`
- ✅ 会话存储：`${userId}_${sessionType}-${sessionId}_session`
- ✅ IndexedDB 数据库名：`ExerciseSolveApp_${userId}`

### 6.2 获取用户ID

**函数**：`getCurrentUserIdOrDefault()`

**位置**：`imates-web/src/utils/user/userId.ts`

**逻辑**：
1. 优先从 localStorage 读取 `userId`
2. 如果不存在，返回默认值 `'default'`

## 七、存储优化策略

### 7.1 防抖保存

**适用场景**：教师通用会话、教师题目会话

**防抖时间**：1秒

**目的**：避免频繁保存，提高性能

**代码示例**：
```typescript
let saveDebounceTimer: ReturnType<typeof setTimeout> | null = null

const saveChatHistory = async (immediate: boolean = false): Promise<void> => {
  if (saveDebounceTimer) {
    clearTimeout(saveDebounceTimer)
    saveDebounceTimer = null
  }
  
  if (immediate) {
    await saveAction()
  } else {
    saveDebounceTimer = setTimeout(saveAction, 1000)
  }
}
```

### 7.2 数据序列化

**目的**：确保所有属性都可以被存储（包括 base64 图片数据、语音数据等）

**序列化内容**：
- 消息基本属性（id, content, sender, type, timestamp）
- 图片数据（包括 base64DataUrl）
- 语音数据
- 错误和重试信息

### 7.3 降级策略

**IndexedDB 不可用时的降级流程**：
1. 尝试保存到 IndexedDB
2. 如果失败，捕获错误
3. 降级到 localStorage
4. 记录警告日志

## 八、存储清理策略

### 8.1 自动清理

**过期时间**：30天（默认）

**清理逻辑**：
```typescript
async cleanupExpiredChatHistory(maxAge: number = 30 * 24 * 60 * 60 * 1000): Promise<void> {
  const keys = await this.getAllChatHistoryKeys()
  const now = Date.now()
  
  for (const key of keys) {
    const data = await userLocalForage.getItem<ChatHistoryData>(key)
    if (data && (now - data.lastUpdated) > maxAge) {
      await userLocalForage.removeItem(key)
    }
  }
}
```

### 8.2 手动清理

**删除单个会话**：
- 删除会话信息（localStorage）
- 删除消息历史（IndexedDB/localStorage）

**清空所有数据**：
- 遍历所有存储键，逐个删除

## 九、关键代码位置

### 9.1 存储服务

| 功能 | 文件 | 方法 |
|-----|------|------|
| 保存消息 | `chat-storage.ts` | `saveChatHistory()` |
| 加载消息 | `chat-storage.ts` | `loadChatHistory()` |
| 删除消息 | `chat-storage.ts` | `removeChatHistory()` |
| 清理过期数据 | `chat-storage.ts` | `cleanupExpiredChatHistory()` |

### 9.2 会话管理

| 会话类型 | 文件 | 关键方法 |
|---------|------|---------|
| 教师通用会话 | `teacherGeneralChatStore.ts` | `saveChatHistory()`, `loadChatHistory()` |
| 教师题目会话 | `teacherExerciseChatStore.ts` | `saveSession()`, `getAllSessions()` |
| AI通用会话 | `aiGeneralChatStore.ts` | `saveSessions()`, `loadSessions()` |
| AI题目会话 | `aiExerciseChatStore.ts` | `saveChatHistory()`, `loadChatHistory()` |
| AI教材会话 | `aiTextbookChatStore.ts` | `saveChatHistory()`, `loadChatHistory()` |

## 十、注意事项

### 10.1 存储键名一致性

⚠️ **重要**：确保所有地方使用相同的键名格式：
- ✅ 正确：`${userId}_teacher-general-${sessionId}_session`
- ❌ 错误：`teacher-general-${sessionId}_session`（缺少 userId 前缀）

### 10.2 存储大小限制

- **localStorage**：通常限制为 5-10MB
- **IndexedDB**：通常限制为 50MB 或更大

建议监控存储使用情况，必要时清理过期数据。

### 10.3 数据迁移

**教师题目会话**支持从旧格式（每个会话单独存储）自动迁移到新格式（统一存储）。

迁移逻辑在 `teacherExerciseChatStore.ts` 的 `migrateOldSessions()` 方法中实现。

### 10.4 消息过滤

保存消息时会自动过滤以下类型的消息：
- 错误消息
- 流式消息
- 系统消息
- 撤回消息
- 无消息ID的消息

这些消息不会保存到持久化存储中。

