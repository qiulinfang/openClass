<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp">

    <TextView
        android:id="@+id/text_catalogue"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_toStartOf="@+id/arrow_view"
        android:layout_centerVertical="true"
        android:textSize="16sp"
        android:textStyle="bold" />

    <ImageView
        android:id="@+id/arrow_view"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:src="@drawable/chat_ai_ic_arrow_right"
        android:layout_toStartOf="@id/btn_more_catalogue"
        android:layout_centerVertical="true"/>

    <ImageView
        android:id="@+id/btn_more_catalogue"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:contentDescription=""
        android:src="@drawable/chat_ai_ic_more_vert"
        android:visibility="invisible" />

</RelativeLayout>