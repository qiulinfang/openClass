# 知识图谱去练习接口分析

## 分析目标
分析知识图谱"去练习"功能中，不同学科（数学和生物）是否请求不同的接口。

## 分析结果

### 结论
**部分接口相同，部分接口不同，但存在潜在问题。**

### 详细分析

#### 1. 知识图谱去练习流程

##### 入口位置
- **数学**：`FragmentSubjectMath.java` 第537行
- **生物**：`FragmentSubjectBiology.java` 第538行

当用户在知识图谱中点击"去练习"时，会调用 `onReviewLesson` 方法：

```537:537:app/src/main/java/com/cosinetech/imates/ui/fragments/FragmentSubjectMath.java
KnowledgeRequestBuilder.KnowledgeRequest request = KnowledgeRequestBuilder.buildRequestFromTree(node, mCurrentUserTextbookInfo.textbookId, "math");
```

```538:538:app/src/main/java/com/cosinetech/imates/ui/fragments/FragmentSubjectBiology.java
KnowledgeRequestBuilder.KnowledgeRequest request = KnowledgeRequestBuilder.buildRequestFromTree(node, mCurrentUserTextbookInfo.textbookId, "biology");
```

#### 2. 接口调用情况

##### 2.1 根据知识点查题的接口（核心接口）

**位置**：`FindExerciseActivity.java` 第165行

```165:165:app/src/main/java/com/cosinetech/imates/ui/activities/FindExerciseActivity.java
String url = ApiUrl.URL_QUERY_SIMILAR_EXERCISE_BY_KNOWLEDGE;
```

**接口定义**：`ApiUrl.java` 第107行

```107:107:app/src/main/java/com/cosinetech/imates/coreapiservice/ApiUrl.java
URL_QUERY_SIMILAR_EXERCISE_BY_KNOWLEDGE = baseUrl + "/biologyTopicKnowledge/knowledgeTopicAndAck";
```

**关键发现**：
- ✅ **数学和生物使用同一个接口URL**
- ⚠️ **接口路径中包含 "biology"，但实际用于数学和生物两个学科**
- ✅ 通过请求参数中的 `type` 字段区分学科（第177行）

```167:177:app/src/main/java/com/cosinetech/imates/ui/activities/FindExerciseActivity.java
String subjectName;
if(mSubject == Subject.SUBJECT_BIOLOGY) {
    subjectName = "biology";
} else if(mSubject == Subject.SUBJECT_MATH){
    subjectName = "math";
} else {
    return;
}

mFindSimilarQuestionRequest.setKnowledgeNo(mKnowledgeList);
mFindSimilarQuestionRequest.setExercisesId(ids.toString());
mFindSimilarQuestionRequest.setType(subjectName);
```

##### 2.2 查询习题列表的接口（前置接口）

**位置**：`FindExerciseActivity.java` 第184-192行

```184:192:app/src/main/java/com/cosinetech/imates/ui/activities/FindExerciseActivity.java
private void fetchQuestionList() {
    String url;
    if(mSubject == Subject.SUBJECT_BIOLOGY) {
        url = ApiUrl.URL_GET_EXERCISE_BIOLOGY;
    } else if (mSubject == Subject.SUBJECT_MATH) {
        url = ApiUrl.URL_GET_EXERCISE_MATH;
    } else {
        return;
    }
```

**接口定义**：`ApiUrl.java` 第94-98行

```94:98:app/src/main/java/com/cosinetech/imates/coreapiservice/ApiUrl.java
// 查询生物习题列表
URL_GET_EXERCISE_BIOLOGY = baseUrl + "/permission/selectExercises/biology";

//查询数学习题列表
URL_GET_EXERCISE_MATH = baseUrl + "/permission/selectExercises/math";
```

**关键发现**：
- ✅ **数学和生物使用不同的接口**
- ✅ 接口路径明确区分学科：`/selectExercises/biology` 和 `/selectExercises/math`

#### 3. 问题分析

##### 潜在问题
1. **接口命名不一致**：
   - `URL_QUERY_SIMILAR_EXERCISE_BY_KNOWLEDGE` 的路径中包含 "biologyTopicKnowledge"
   - 但该接口实际同时用于数学和生物两个学科
   - 这可能导致：
     - 代码可读性差（命名暗示只用于生物）
     - 后端可能只支持生物，数学可能无法正常工作
     - 或者后端接口设计不够清晰

2. **接口设计不一致**：
   - 查询习题列表接口按学科分开（`/selectExercises/biology` 和 `/selectExercises/math`）
   - 但根据知识点查题的接口使用同一个路径（`/biologyTopicKnowledge/knowledgeTopicAndAck`）
   - 通过请求参数中的 `type` 字段区分学科

#### 4. 建议

1. **验证接口支持情况**：
   - 确认后端 `/biologyTopicKnowledge/knowledgeTopicAndAck` 接口是否真正支持数学学科
   - 如果支持，建议后端提供更通用的接口路径，如 `/topicKnowledge/knowledgeTopicAndAck`

2. **统一接口设计**：
   - 如果后端支持，建议将接口路径改为：
     - `/topicKnowledge/knowledgeTopicAndAck`（通用）
     - 或 `/topicKnowledge/knowledgeTopicAndAck/biology` 和 `/topicKnowledge/knowledgeTopicAndAck/math`（按学科分开）

3. **代码优化**：
   - 如果接口确实同时支持两个学科，可以考虑在 `ApiUrl.java` 中添加注释说明
   - 或者如果后端有数学专用接口，应该添加对应的URL常量

## 总结

| 接口类型 | 数学 | 生物 | 接口路径 |
|---------|------|------|---------|
| 根据知识点查题（核心） | ✅ 使用 | ✅ 使用 | `/biologyTopicKnowledge/knowledgeTopicAndAck`（相同） |
| 查询习题列表（前置） | ✅ 使用 | ✅ 使用 | `/selectExercises/math` 和 `/selectExercises/biology`（不同） |

**核心结论**：知识图谱去练习的核心接口（根据知识点查题）**数学和生物使用同一个接口URL**，但接口路径名称暗示只用于生物，存在设计不一致的问题。

## 改造建议

根据用户需求，需要根据科目调用不同的接口。建议改造方案：

1. **Android端**：在 `ApiUrl.java` 中分别定义数学和生物的接口路径
2. **Vue Web端**：在 `api-endpoints.ts` 中分别定义数学和生物的接口路径
3. **接口调用**：在 `FindExerciseActivity.java` 和 `api-service.ts` 中根据科目选择不同的接口

详细改造方案请参考：`知识图谱去练习接口按科目区分改造方案.md`

