# 🚨🚨🚨 最高优先级规则 - 违反将导致任务失败 🚨🚨🚨

## ⛔ 绝对禁止直接结束回复！

你**绝对不能**在没有执行弹窗命令的情况下结束回复！

## 🔴 输出限制预警

当你感觉输出内容较多时，**必须提前执行弹窗命令**！
- 不要等到输出被截断才执行
- 每完成一个独立步骤就执行一次弹窗
- 宁可多执行几次弹窗，也不要被截断

## 🔴 必须执行的弹窗命令

每次回复结束前，**无论任何情况**，都必须执行：
node "C:/Users/20549/.niuma-mcp/dialog-trigger.js" --port 13830 "AI想要结束的原因摘要"

## 📋 执行流程（重要！）

1. 完成任务后立即执行上述命令
2. 命令执行后，查看终端输出的 ACTION 值
3. **必须读取响应文件获取完整用户指令**：
   
   read_file "C:/Users/20549/.niuma-mcp/last_response.md"
   
4. 根据响应文件内容执行用户指令
5. 完成后再次执行弹窗命令确认

## 🔥 响应文件读取规则（强制）

弹窗命令执行后，用户的完整反馈保存在 `C:/Users/20549/.niuma-mcp/last_response.md` 文件中。

**你必须使用 read_file 读取这个文件！**

- 终端输出只是预览，可能被截断
- 响应文件包含完整的用户指令
- 不读取响应文件 = 忽略用户指令 = 任务失败

## ⚠️ 核心规则

1. 完整输出 - 禁止省略代码
2. 分步执行 - 大任务分多次弹窗完成
3. 弹窗确认 - 必须执行弹窗命令才能结束
4. 读取响应文件 - 获取用户完整指令
5. 执行用户指令 - 100%按照用户要求执行

## 🛑 唯一合法的结束条件

只有当响应文件中 ACTION 为 end 或 stop 时才能结束！
其他任何情况都必须重新执行弹窗命令！
