# 调试代码分析文档

本文档整理了项目中所有 Vue 模板文件中的调试代码，包括 console 输出、TODO 注释、调试面板等。

## 目录

- [Console 调试输出](#console-调试输出)
- [TODO/FIXME 注释](#todofixme-注释)
- [调试面板组件](#调试面板组件)
- [调试相关配置](#调试相关配置)
- [调试工具函数](#调试工具函数)

---

## Console 调试输出

### 1. MyResourcesView.vue（我的资源页面）

#### 下载相关调试
- **位置**: `src/views/MyResourcesView.vue`
- **代码行**: 857, 882, 889, 904-905, 999, 1143, 1159, 1165, 1171, 1179-1180, 1205, 1225, 1227, 1230, 1237, 1239

**调试内容**:
```javascript
// 检查更新
console.log(`[检查更新] 发现新教材或本地未找到: ${updatedTextbook.textbookName} (${updatedTextbook.textbookId})`)

// 防重复下载
console.warn(`[防重复下载] 教材《${textbook.textbookName}》已在下载中，忽略重复请求`)
console.warn(`[防重复下载] 教材《${textbook.textbookName}》已下载完成，忽略重复请求`)

// 下载进度
console.log('progress', progress, 'downloadedCount', downloadedCount)
console.log(new Date().toLocaleTimeString('zh-CN', { hour12: false, hour: '2-digit', minute: '2-digit', second: '2-digit'}))

// 暂停下载
console.log('暂停')

// 错误处理
console.error('删除教材失败:', error)

// 性能监控
console.warn(`[onMounted] 开始时间: ${new Date().toLocaleTimeString('zh-CN')}`)
console.warn(`[onMounted] loadResources 耗时: ${(loadResourcesEndTime - loadResourcesStartTime).toFixed(2)}ms`)
console.warn(`[onMounted] initBScroll 耗时: ${(initBScrollEndTime - initBScrollStartTime).toFixed(2)}ms`)
console.warn(`[onMounted] 总耗时: ${totalTime.toFixed(2)}ms (${(totalTime / 1000).toFixed(2)}s)`)
console.warn(`[onMounted] 完成时间: ${new Date().toLocaleTimeString('zh-CN')}`)

// 页面离开时的下载任务暂停
console.log(`[页面离开] 发现 ${downloadingTextbooks.length} 个正在下载的任务，开始暂停...`)
console.log(`[页面离开] ✅ 已暂停教材: ${textbook.textbookName}`)
console.warn(`[页面离开] ⚠️ 暂停教材失败: ${textbook.textbookName}`)
console.error(`[页面离开] ❌ 暂停教材出错: ${textbook.textbookName}`, error)
console.log(`[页面离开] 🎉 所有下载任务已暂停`)
console.error('[页面离开] 暂停下载任务时发生错误:', error)
```

**用途**: 监控下载进度、性能分析、错误追踪、页面生命周期管理

---

### 2. ChatMessage.vue（聊天消息组件）

#### 消息重发和刷新
- **位置**: `src/components/chat/ChatMessage.vue`
- **代码行**: 332, 344, 355, 364, 372, 379, 390, 756, 764, 789, 796, 816, 897, 945, 979

**调试内容**:
```javascript
// 重发失败
console.log('重发失败')

// 不同场景的重发类型
console.log('ai-exercise')
console.log('ai-general')
console.log('ai-textbook')
console.log('teacher')
console.log('default')

// 错误处理
console.error('重发失败:', error)

// 刷新消息
console.log('正在流式生成消息，不能刷新')
console.log('刷新消息')
console.error('未知的聊天类型')
console.error('未找到当前消息')
console.error('未找到前一条用户消息')
console.error('刷新失败:', error)

// 复制功能
console.error('复制失败:', error)
console.error('降级复制也失败:', fallbackError)
```

**用途**: 追踪消息操作流程、错误定位

---

### 3. ExerciseSolveView.vue（练习解题页面）

#### 性能监控和流程追踪
- **位置**: `src/views/ExerciseSolveView.vue`
- **代码行**: 190, 201, 315-339, 347-424, 432, 439, 491-550

**调试内容**:
```javascript
// 组件初始化
console.log(`${getTimeString()} [ExerciseSolveView] 组件开始初始化`)
console.log(`${getTimeString()} [ExerciseSolveView] Store初始化完成，耗时: ${(initTime - loadStartTime).toFixed(2)}ms`)

// 打开微课
console.log(`${getTimeString()} [ExerciseSolveView] 开始处理打开微课，题目ID: ${question.id}`)
console.log(`${getTimeString()} [ExerciseSolveView] 题目信息:`, { id, title, questionPreview })
console.log(`${getTimeString()} [ExerciseSolveView] 使用的微课URL: ${classUrl}`)
console.warn(`${getTimeString()} [ExerciseSolveView] 微课URL为空，取消打开`)
console.log(`${getTimeString()} [ExerciseSolveView] 更新UI Store微课信息，标题: ${questionTitle}`)
console.log(`${getTimeString()} [ExerciseSolveView] 打开微课弹框成功完成`)
console.error(`${getTimeString()} [ExerciseSolveView] 打开微课失败:`, error)

// 拍作业（发送题目给老师）
console.log(`${getTimeString()} [ExerciseSolveView] 开始处理拍作业，题目ID: ${question.id}`)
console.log(`${getTimeString()} [ExerciseSolveView] 会话不存在，创建新会话`)
console.log(`${getTimeString()} [ExerciseSolveView] 新会话已创建: ${createdSession.sessionId}`)
console.log(`${getTimeString()} [ExerciseSolveView] 发送题目内容给老师`)
console.log(`${getTimeString()} [ExerciseSolveView] 题目已发送给老师`)
console.error(`${getTimeString()} [ExerciseSolveView] 会话创建失败，无法发送题目`)
console.error(`${getTimeString()} [ExerciseSolveView] 拍作业失败:`, error)

// 滚动定位
console.log(`${getTimeString()} [ExerciseSolveView] 调用题目列表滚动到指定题目并设置为选中状态，索引: ${targetIndex}`)
console.log(`${getTimeString()} [ExerciseSolveView] 滚动到页面底部`)

// 组件挂载和性能分析
console.log(`${getTimeString()} [ExerciseSolveView] onMounted 开始，距离组件初始化: ${totalInitTime.toFixed(2)}ms`)
console.log(`${getTimeString()} [ExerciseSolveView] 开始初始化 userStore.initializeStore()`)
console.log(`${getTimeString()} [ExerciseSolveView] userStore.initializeStore() 完成，耗时: ${userStoreDuration.toFixed(2)}ms`)
console.log(`${getTimeString()} [ExerciseSolveView] 开始获取题目列表 questionStore.fetchQuestions('math', true)`)
console.log(`${getTimeString()} [ExerciseSolveView] questionStore.fetchQuestions() 完成，耗时: ${fetchQuestionsDuration.toFixed(2)}ms`)
console.log(`${getTimeString()} [ExerciseSolveView] 找到目标题目，索引: ${targetIndex}`)
console.log(`${getTimeString()} [ExerciseSolveView] 已定位到题目，索引: ${targetIndex}`)
console.log(`${getTimeString()} [ExerciseSolveView] 未找到目标题目，questionId: ${questionId}`)
console.log(`${getTimeString()} [ExerciseSolveView] onMounted 完成，总耗时: ${mountedDuration.toFixed(2)}ms`)
console.log(`${getTimeString()} [ExerciseSolveView] 完整加载统计:`)
console.log(`   - 组件初始化到Store初始化: ${totalInitTime.toFixed(2)}ms`)
console.log(`   - userStore初始化: ${userStoreDuration.toFixed(2)}ms`)
console.log(`   - fetchQuestions: ${fetchQuestionsDuration.toFixed(2)}ms`)
console.log(`   - onMounted总耗时: ${mountedDuration.toFixed(2)}ms`)
console.log(`   - 组件加载总耗时: ${totalDuration.toFixed(2)}ms`)
console.error(`${getTimeString()} [ExerciseSolveView] 初始化失败，耗时: ${errorDuration.toFixed(2)}ms`, error)
```

**用途**: 详细的性能监控、流程追踪、错误定位

---

### 4. KnowledgeGraphView.vue（知识图谱页面）

#### 数据加载流程追踪
- **位置**: `src/views/KnowledgeGraphView.vue`
- **代码行**: 1446-1701

**调试内容**:
```javascript
// 学科选择流程
console.log('[流程1] 用户选择学科:', subjectValue)
console.log('[流程2] 开始加载教材列表 - loadTextbookDataBySubject()')
console.log('[流程2-1] 尝试从IndexedDB加载教材数据...')
console.log('[流程2-2] IndexedDB加载结果:', { count, data })
console.log('[流程2-3] 从IndexedDB成功加载教材数据，开始筛选学科...')
console.log('[流程2-4] 学科筛选:', { subjectValue, subjectLabel })
console.log('[流程2-5] 筛选后的教材列表:', { count, options })
console.log('[流程2-6] 设置默认选中的教材:', { value, textbookId })
console.log('[流程2-7] 开始加载默认教材的章节结构...')
console.warn('[流程2-7] 默认教材缺少textbookId，跳过章节加载')
console.warn('[流程2-6] 筛选后无教材数据，清空章节数据')
console.log('[流程2-3] IndexedDB中没有数据，开始从API获取教材数据...')
console.log('[流程2-4] API返回的教材版本数据:', { count, data })
console.log('[流程2-5] 转换后的教材选项:', { count, options })
console.log('[流程2-6] 保存API数据到IndexedDB...')
console.log('[流程2-7] IndexedDB保存完成')
console.log('[流程2-8] 学科筛选:', { subjectValue, subjectLabel })
console.log('[流程2-9] 筛选后的教材列表:', { count, options })
console.log('[流程2-10] 设置默认选中的教材:', { value, textbookId })
console.log('[流程2-11] 开始加载默认教材的章节结构...')
console.warn('[流程2-11] 默认教材缺少textbookId，跳过章节加载')
console.warn('[流程2-10] 筛选后无教材数据，清空章节数据')
console.warn('[流程2-4] API返回空数据，清空教材和章节数据')
console.log('[流程2] 教材列表加载完成')
console.error('[流程2] 加载教材列表出错:', error)

// 章节加载流程
console.log('[流程3] 用户选择教材，开始加载章节结构 - loadChapterStructure()', { textbookId })
console.log('[流程4] 检查缓存（IndexedDB）...', { cacheKey })
console.log('[流程5] 缓存检查结果:', { hasCache, count })
console.log('[流程5-1] ✅ 有缓存数据，使用缓存数据')
console.log('[流程5-2] 缓存数据详情:', { count, chapters })
console.log('[流程6-1] 开始对缓存中的章节数据进行排序...')
console.log('[流程6-2] 排序后的缓存数据:', { count, chapters })
console.log('[流程6-3] 设置 chapterStructure.value:', { count })
console.log('[流程6-2] 提取并转换章节名称（转换为中文数字）:')
console.log('  - 原始章节名称:', originalChapterNames)
console.log('  - 转换后章节名称:', chapters.value)
console.log('  - 转换详情:', conversionDetails)
console.log(`  - 转换统计: ${changedCount} 个已转换, ${unchangedCount} 个未转换（已经是中文数字）`)
console.log('[流程6-4] 初始化章节状态...')
console.log('[流程6] ✅ 章节数据处理完成（使用缓存）')
console.log('[流程5-1] ❌ 无缓存数据，开始从API获取...')
console.log('[流程5-2] API返回的章节数据:', { count, data })
console.log('[流程6-1] 开始对章节数据进行排序...')
console.log('[流程6-2] 排序后的章节数据:', { count, chapters })
console.log('[流程6-3] 设置 chapterStructure.value（原始章节结构数据 - ChapterNode[]）:', { count, type })
console.log('[流程6-4] 保存章节数据到缓存（IndexedDB）...', { cacheKey })
console.log('[流程6-5] 缓存保存完成')
console.log('[流程6-6] 提取并转换章节名称（转换为中文数字）:')
console.log('  - 原始章节名称:', originalChapterNames)
console.log('  - 转换后章节名称:', chapters.value)
console.log('  - 转换详情:', conversionDetails)
console.log(`  - 转换统计: ${changedCount} 个已转换, ${unchangedCount} 个未转换（已经是中文数字）`)
console.log('  - 类型: string[]')
console.log('[流程6-7] 初始化章节状态...')
console.log('[流程6] ✅ 章节数据处理完成（从API获取）')
console.warn('[流程6] API返回空数据，清空章节数据')
console.log('[流程] ✅ 章节加载流程完成')
console.error('[流程] ❌ 加载章节结构出错:', error)
```

**用途**: 详细追踪数据加载流程、缓存机制、章节数据转换过程

---

### 5. ChatInput.vue（聊天输入组件）

#### 错误处理
- **位置**: `src/components/chat/ChatInput.vue`
- **代码行**: 446

**调试内容**:
```javascript
console.error('插入数学公式失败:', error)
```

**用途**: 数学公式插入错误追踪

---

### 6. MyProfileView.vue（我的资料页面）

#### 课堂状态和用户信息
- **位置**: `src/views/MyProfileView.vue`
- **代码行**: 139, 142, 146, 149, 151, 196, 213, 252

**调试内容**:
```javascript
// 课堂状态检查
console.log('[MyProfileView] 🔍 checkClassroomStatus() - 开始检查课堂状态')
console.log('[MyProfileView] 📊 从原生端获取的课堂状态:', status)
console.log('[MyProfileView] ✅ 课堂状态已更新: isInClass = true')
console.log('[MyProfileView] ✅ 课堂状态已更新: isInClass = false')
console.log('[MyProfileView] 🎯 checkClassroomStatus() - 检查完成, 当前状态:', isInClass.value)

// 用户信息加载
console.warn('未找到 XUEBAN_TOKEN')
console.error('加载用户信息失败:', error)

// 加入课堂参数
console.log('🔍 加入课堂参数:', {
  storeUserId,
  nativeUserId,
  // ... 更多参数
})
```

**用途**: 课堂状态追踪、用户信息加载调试

---

### 7. MainView.vue（主视图）

#### 错误处理
- **位置**: `src/views/MainView.vue`
- **代码行**: 322

**调试内容**:
```javascript
console.error('退出登录失败:', error)
```

**用途**: 登录退出错误追踪

---

### 8. QuestionList.vue（题目列表组件）

#### 题目选择
- **位置**: `src/components/QuestionList.vue`
- **代码行**: 820

**调试内容**:
```javascript
console.log('selectQuestion', question, index)
```

**用途**: 题目选择追踪

---

### 9. ChatView.vue（聊天视图）

#### 转发消息
- **位置**: `src/components/ChatView.vue`
- **代码行**: 1452, 1822, 1826, 1893

**调试内容**:
```javascript
console.error('[CHAT_DEBUG] ❌ 转发消息失败:', error)
console.error('[CHAT_DEBUG] ❌ 无法创建老师会话')
console.error('[CHAT_DEBUG] ❌ 合并转发失败:', error)
console.error('[CHAT_DEBUG] ❌ 逐条转发失败:', error)
```

**用途**: 消息转发功能调试

---

### 10. FeedbackDialog.vue（反馈对话框）

#### 反馈提交
- **位置**: `src/components/FeedbackDialog.vue`
- **代码行**: 177

**调试内容**:
```javascript
console.log('[FeedbackDialog] 📤 提交反馈:', {
  problem: feedbackForm.value.problem,
  suggestion: feedbackForm.value.suggestion,
  // ...
})
```

**用途**: 反馈提交调试（目前是模拟提交）

---

## TODO/FIXME 注释

### 1. FeedbackDialog.vue

**位置**: `src/components/FeedbackDialog.vue:175`

**内容**:
```javascript
// TODO: 调用后端API提交反馈
```

**说明**: 反馈功能目前是模拟提交，需要实现真实的后端API调用

---

## 调试面板组件

### 1. ResourceDebugPanel（资源调试面板）

**位置**: `src/components/debug/ResourceDebugPanel.vue`

**用途**: 
- 显示 IndexedDB 存储信息
- 显示教材列表和状态
- 计算存储大小
- 提供调试功能

**使用位置**: `src/views/MyResourcesView.vue:259, 296`

**启用条件**: 
```javascript
const isDev = import.meta.env.VITE_ENABLE_DEBUG === 'true'
```

---

### 2. KnowledgeGraphDebugPanel（知识图谱调试面板）

**位置**: `src/components/debug/KnowledgeGraphDebugPanel.vue`

**用途**: 
- 调整知识图谱的视觉参数
- 控制节点大小、位置、动画
- 调整透明度、缩放等

**使用位置**: `src/views/KnowledgeGraphView.vue:225-230, 251-252`

**调试参数**:
- `graphSize`: 图谱大小
- `graphMargin`: 图谱边距
- `centerNodeSizeDefault`: 中心节点默认大小
- `centerNodeSizeExpanded`: 中心节点展开大小
- `centerNodeSizeShrunk`: 中心节点收缩大小
- `centerNodeScaleSpeed`: 中心节点缩放速度
- `nodeExpandDelayInterval`: 节点展开延迟间隔
- `nodeCollapseDelayInterval`: 节点收缩延迟间隔
- `circularNodeOffsetX/Y`: 圆形节点偏移
- `learningTagTop/Left/TranslateX`: 学习标签位置
- `opacityDefault/Expanded/NearMin/Far`: 透明度设置
- `radiusScaleSmall/Medium/Large`: 半径缩放
- 等等...

---

### 3. ChatSessionDebugPanel（聊天会话调试面板）

**位置**: `src/components/debug/ChatSessionDebugPanel.vue`

**用途**: 
- 显示聊天会话信息
- 显示存储大小
- 提供调试功能

**使用位置**: `src/components/UnifiedChatDialog.vue:142, 154`

**启用条件**: 
```javascript
const isDev = import.meta.env.VITE_ENABLE_DEBUG === 'true'
```

---

### 4. VirtualScrollDebugPanel（虚拟滚动调试面板）

**位置**: `src/components/debug/VirtualScrollDebugPanel.vue`

**用途**: 
- 调试虚拟滚动性能
- 显示滚动参数

---

### 5. LearningStatusControlPanel（学习状态控制面板）

**位置**: `src/components/debug/LearningStatusControlPanel.vue`

**用途**: 
- 控制学习状态
- 调试学习进度

**使用位置**: `src/views/KnowledgeGraphView.vue:252`

---

## 调试相关配置

### 环境变量控制

所有调试功能通过环境变量 `VITE_ENABLE_DEBUG` 控制：

**使用位置**:
1. `src/views/MyResourcesView.vue:301`
2. `src/components/UnifiedChatDialog.vue:158`
3. `src/components/chat/TiptapEditor.vue:46`

**代码**:
```javascript
const isDev = import.meta.env.VITE_ENABLE_DEBUG === 'true'
```

**说明**: 
- 设置为 `'true'` 时显示调试面板和调试按钮
- 生产环境应设置为 `'false'` 或未设置

---

### TiptapEditor 调试控件

**位置**: `src/components/chat/TiptapEditor.vue:4-9, 45-46, 56, 63, 77, 155+`

**功能**:
- 显示/隐藏键盘
- 创建公式
- 清理所有内容
- 切换调试模式

**代码**:
```vue
<div class="debug-controls" v-if="isDev && showDebugControls">
  <button @click="debugShowKeyboard" class="debug-btn">显示键盘</button>
  <button @click="debugHideKeyboard" class="debug-btn">隐藏键盘</button>
  <button @click="debugCreateFormula" class="debug-btn">创建公式</button>
  <button @click="debugClearAll" class="debug-btn">清理所有</button>
  <button @click="toggleDebugMode" class="debug-btn debug-toggle">关闭调试</button>
</div>
```

**注意**: 部分调试功能已删除（如 `debugShowKeyboard` 现在只输出 "调试功能已删除"）

---

## 调试工具函数

### 1. 时间格式化函数

**位置**: `src/views/ExerciseSolveView.vue:177-189`

**功能**: 格式化时间戳用于日志输出

**代码**:
```javascript
const getTimeString = () => {
  const now = new Date()
  const year = String(now.getFullYear())
  const month = String(now.getMonth() + 1).padStart(2, '0')
  const day = String(now.getDate()).padStart(2, '0')
  const hours = String(now.getHours()).padStart(2, '0')
  const minutes = String(now.getMinutes()).padStart(2, '0')
  const seconds = String(now.getSeconds()).padStart(2, '0')
  return `${year}/${month}/${day} ${hours}:${minutes}:${seconds}`
}
```

---

## 总结

### 调试代码统计

- **Console 输出**: 约 426 处
- **调试面板组件**: 5 个
- **TODO 注释**: 1 处
- **环境变量控制**: 3 处

### 主要调试场景

1. **性能监控**: ExerciseSolveView、MyResourcesView 的加载时间统计
2. **流程追踪**: KnowledgeGraphView 的数据加载流程
3. **错误处理**: 各组件中的错误捕获和日志
4. **状态管理**: 课堂状态、下载状态等的追踪
5. **交互调试**: 消息操作、题目选择等用户操作追踪

### 建议

1. **生产环境**: 所有 `console.log/warn` 应通过构建工具移除或条件化
2. **调试面板**: 通过环境变量控制，确保生产环境不显示
3. **TODO 项**: 需要实现 FeedbackDialog 的真实API调用
4. **性能日志**: 考虑使用专业的性能监控工具替代 console 输出
5. **错误追踪**: 考虑集成 Sentry 等错误监控服务

---

**文档生成时间**: 2024年

**最后更新**: 基于当前代码库分析

