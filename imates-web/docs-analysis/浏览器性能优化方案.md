# 浏览器性能优化方案

## 📋 概述

本文档总结了项目中已有的性能优化措施，并提供了针对浏览器性能低问题的综合解决方案。

---

## 一、项目中已有的性能优化措施

### 1.1 防抖和节流优化

**位置**：`src/utils/common/throttle.ts`

**已实现功能**：
- ✅ 节流函数（throttle）- 限制函数执行频率
- ✅ 防抖函数（debounce）- 延迟执行最后一次调用
- ✅ 预设配置（快速/标准/慢速/极慢）

**使用场景**：
- PDF 渲染防抖（`PdfPage.vue`）
- 笔记保存防抖（`pdfViewerStore.ts`）
- 聊天输入防抖（`ChatInput.vue`）
- 知识图谱交互防抖（`KnowledgeGraphView.vue`）

**优化效果**：
- 减少不必要的函数调用
- 降低 CPU 占用
- 提升滚动和交互流畅度

---

### 1.2 缓存策略

#### 1.2.1 请求缓存

**位置**：`src/services/api-service.ts`

**实现**：
```typescript
private requestCache = new Map<string, { data: unknown, timestamp: number }>()
private readonly CACHE_TTL = 5 * 60 * 1000 // 5分钟缓存
```

**优化效果**：
- 减少重复网络请求
- 提升响应速度
- 降低服务器压力

#### 1.2.2 渲染缓存

**位置**：`src/composables/useMessageRenderer.ts`

**实现**：
```typescript
const renderCache = new Map<string, string>()
const MAX_CACHE_SIZE = 100
```

**优化效果**：
- 避免重复渲染相同内容
- 减少 DOM 操作
- 提升渲染性能

#### 1.2.3 本地存储缓存

**位置**：`KnowledgeGraphView.vue`, `MyProfileView.vue`

**实现**：
- localStorage 缓存用户信息
- IndexedDB 缓存题目数据
- 24小时过期策略

**优化效果**：
- 离线可用
- 减少网络请求
- 提升加载速度

---

### 1.3 懒加载策略

#### 1.3.1 消息懒加载

**位置**：`src/utils/render/lazy-message-renderer.ts`

**实现**：
- 使用 Intersection Observer 监听元素进入视口
- 批量处理渲染队列（每批5个，延迟16ms）
- 提前50px开始渲染

**优化效果**：
- 只渲染可见内容
- 减少初始渲染时间
- 降低内存占用

#### 1.3.2 题目列表懒加载

**位置**：`QuestionList.vue`

**实现**：
- 初始显示20个题目
- 滚动到底部加载更多
- 渐进式渲染（前3个立即渲染）

**优化效果**：
- 快速显示首屏内容
- 按需加载数据
- 提升用户体验

---

### 1.4 代码分割优化

**位置**：`vite.config.ts`

**实现**：
```typescript
manualChunks: {
  'vendor': ['vue', 'vue-router', 'pinia'],
  'quasar': ['quasar'],
  'mathlive': ['mathlive'],
  'tiptap': ['@tiptap/core', '@tiptap/vue-3', '@tiptap/starter-kit']
}
```

**优化效果**：
- 减少初始包大小
- 按需加载代码
- 提升首屏加载速度

---

### 1.5 PDF 渲染优化

**位置**：`src/services/pdf/core/PdfCoreService.ts`, `src/components/PdfPage.vue`

**实现**：
- 高 DPI 支持（devicePixelRatio）
- 渲染任务取消机制
- 防抖渲染

**优化效果**：
- 提升渲染质量
- 避免重复渲染
- 降低内存占用

---

## 二、可以进一步优化的点

### 2.1 HTTP 客户端优化

**当前问题**（`src/services/http-client.ts`）：

1. **重复的 URL 构建**
   - `buildFullUrl()` 每次请求都会执行路由匹配
   - 可以缓存路由映射结果

2. **动态导入开销**
   - `getDynamicAuthConfig()` 和 `clearTokenByPath()` 使用动态导入
   - 每次请求都会执行动态导入

3. **重试机制**
   - 当前使用递增延迟（1s, 2s, 3s）
   - 可以优化为指数退避

**优化方案**：

```typescript
// 1. 缓存路由映射结果
private routeCache = new Map<string, string>()

public buildFullUrl(url: string): string {
  if (this.routeCache.has(url)) {
    return this.routeCache.get(url)!
  }
  
  // ... 原有逻辑 ...
  
  const result = `${matchedBase}${url}`
  this.routeCache.set(url, result)
  return result
}

// 2. 预加载认证服务（避免动态导入）
private authStorageService: typeof import('./auth-storage-service') | null = null

private async ensureAuthService() {
  if (!this.authStorageService) {
    this.authStorageService = await import('./auth-storage-service')
  }
  return this.authStorageService
}

// 3. 优化重试延迟（指数退避）
const retryDelay = Math.min(1000 * Math.pow(2, attempt), 10000) // 最大10秒
```

---

### 2.2 构建优化

**当前配置**（`vite.config.ts`）：

**可以优化的点**：

1. **压缩配置**
   ```typescript
   build: {
     minify: 'terser', // 使用 terser 获得更好的压缩率
     terserOptions: {
       compress: {
         drop_console: true, // 生产环境移除 console
         drop_debugger: true
       }
     }
   }
   ```

2. **资源优化**
   ```typescript
   build: {
     assetsInlineLimit: 4096, // 可以提高到 8192
     rollupOptions: {
       output: {
         // 优化资源命名
         assetFileNames: 'assets/[name]-[hash:8].[ext]',
         // 启用 tree-shaking
         treeshake: true
       }
     }
   }
   ```

3. **预加载关键资源**
   ```typescript
   build: {
     rollupOptions: {
       output: {
         // 预加载关键 chunk
         manualChunks: {
           'vendor': ['vue', 'vue-router', 'pinia'],
           'quasar': ['quasar'],
           // 分离大型库
           'pdf': ['pdfjs-dist'],
           'konva': ['konva']
         }
       }
     }
   }
   ```

---

### 2.3 运行时优化

#### 2.3.1 虚拟滚动

**适用场景**：题目列表、聊天消息列表

**实现方案**：
```typescript
// 使用 vue-virtual-scroller 或 vue-virtual-scroll-list
import { RecycleScroller } from 'vue-virtual-scroller'

// 只渲染可见区域的项目
<RecycleScroller
  class="scroller"
  :items="items"
  :item-size="100"
  key-field="id"
  v-slot="{ item }"
>
  <QuestionCard :question="item" />
</RecycleScroller>
```

**优化效果**：
- 只渲染可见 DOM 元素
- 大幅降低内存占用
- 提升滚动性能

#### 2.3.2 图片懒加载

**当前问题**：图片可能一次性加载

**优化方案**：
```typescript
// 使用原生 loading="lazy" 或 Intersection Observer
<img 
  :src="imageSrc" 
  loading="lazy"
  alt="..."
/>

// 或使用 Intersection Observer
const imageObserver = new IntersectionObserver((entries) => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      const img = entry.target as HTMLImageElement
      img.src = img.dataset.src || ''
      imageObserver.unobserve(img)
    }
  })
})
```

#### 2.3.3 Web Worker

**适用场景**：PDF 处理、大量数据处理

**实现方案**：
```typescript
// 创建 worker
const worker = new Worker(new URL('./pdf-worker.ts', import.meta.url))

// 在 worker 中处理 PDF
worker.postMessage({ type: 'processPdf', data: pdfData })
worker.onmessage = (e) => {
  // 处理结果
}
```

**优化效果**：
- 不阻塞主线程
- 提升 UI 响应速度
- 充分利用多核 CPU

---

### 2.4 内存优化

#### 2.4.1 及时清理引用

**问题**：某些组件可能持有大量数据引用

**优化方案**：
```typescript
// 组件卸载时清理
onUnmounted(() => {
  // 清理事件监听
  // 清理定时器
  // 清理缓存
  // 清理引用
  largeData.value = null
})
```

#### 2.4.2 使用 WeakMap/WeakSet

**当前问题**：某些地方使用 Map/Set 可能导致内存泄漏

**优化方案**：
```typescript
// 如果 key 是对象，使用 WeakMap
const cache = new WeakMap<HTMLElement, string>()

// 如果只需要标记，使用 WeakSet
const renderedElements = new WeakSet<HTMLElement>()
```

---

### 2.5 网络优化

#### 2.5.1 请求合并

**场景**：多个相似的请求可以合并

**优化方案**：
```typescript
// 批量请求接口
async batchRequest(urls: string[]) {
  const promises = urls.map(url => this.get(url))
  return Promise.all(promises)
}
```

#### 2.5.2 请求优先级

**优化方案**：
```typescript
// 使用 AbortController 取消低优先级请求
const controller = new AbortController()

// 高优先级请求
fetch('/api/important', { signal: controller.signal })

// 取消低优先级请求
controller.abort()
```

#### 2.5.3 HTTP/2 Server Push

**配置**：服务器端配置 HTTP/2 Server Push，预推送关键资源

---

## 三、性能监控

### 3.1 性能指标监控

**实现方案**：
```typescript
// 使用 Performance API
const observer = new PerformanceObserver((list) => {
  for (const entry of list.getEntries()) {
    console.log('性能指标:', entry.name, entry.duration)
  }
})

observer.observe({ entryTypes: ['measure', 'navigation', 'resource'] })

// 测量关键操作
performance.mark('start')
// ... 操作 ...
performance.mark('end')
performance.measure('operation', 'start', 'end')
```

### 3.2 错误监控

**实现方案**：
```typescript
// 全局错误监控
window.addEventListener('error', (event) => {
  // 上报错误
  console.error('错误:', event.error)
})

// Promise 错误监控
window.addEventListener('unhandledrejection', (event) => {
  console.error('未处理的 Promise 错误:', event.reason)
})
```

---

## 四、优化优先级建议

### 高优先级（立即实施）

1. **HTTP 客户端优化**
   - 缓存路由映射结果
   - 预加载认证服务
   - 优化重试机制

2. **构建优化**
   - 启用 terser 压缩
   - 移除生产环境 console
   - 优化代码分割

3. **图片懒加载**
   - 添加 `loading="lazy"` 属性
   - 使用 Intersection Observer

### 中优先级（近期实施）

1. **虚拟滚动**
   - 题目列表虚拟滚动
   - 聊天消息列表虚拟滚动

2. **Web Worker**
   - PDF 处理移到 Worker
   - 大量数据处理移到 Worker

3. **内存优化**
   - 及时清理引用
   - 使用 WeakMap/WeakSet

### 低优先级（长期优化）

1. **性能监控**
   - 集成性能监控工具
   - 建立性能基线

2. **网络优化**
   - 请求合并
   - 请求优先级管理

---

## 五、性能优化检查清单

### 5.1 代码层面

- [ ] 使用防抖/节流限制频繁操作
- [ ] 实现请求缓存机制
- [ ] 使用懒加载减少初始渲染
- [ ] 及时清理事件监听和定时器
- [ ] 避免内存泄漏（WeakMap/WeakSet）
- [ ] 使用虚拟滚动处理长列表

### 5.2 构建层面

- [ ] 代码分割（manualChunks）
- [ ] 压缩代码（terser）
- [ ] Tree-shaking
- [ ] 移除生产环境调试代码
- [ ] 优化资源内联策略

### 5.3 运行时层面

- [ ] 图片懒加载
- [ ] 使用 Web Worker 处理重任务
- [ ] 优化 PDF 渲染性能
- [ ] 实现虚拟滚动
- [ ] 监控性能指标

### 5.4 网络层面

- [ ] 请求缓存
- [ ] 请求合并
- [ ] 请求优先级管理
- [ ] 使用 HTTP/2
- [ ] CDN 加速

---

## 六、性能测试工具

### 6.1 Chrome DevTools

- **Performance 面板**：分析运行时性能
- **Memory 面板**：分析内存使用
- **Network 面板**：分析网络请求
- **Lighthouse**：综合性能评分

### 6.2 性能指标

- **FCP (First Contentful Paint)**：首次内容绘制 < 1.8s
- **LCP (Largest Contentful Paint)**：最大内容绘制 < 2.5s
- **FID (First Input Delay)**：首次输入延迟 < 100ms
- **CLS (Cumulative Layout Shift)**：累积布局偏移 < 0.1
- **TTI (Time to Interactive)**：可交互时间 < 3.8s

---

## 七、总结

### 7.1 已实现的优化

✅ 防抖和节流  
✅ 多层缓存策略  
✅ 懒加载机制  
✅ 代码分割  
✅ PDF 渲染优化  

### 7.2 待优化的点

🔲 HTTP 客户端优化  
🔲 构建优化（terser、tree-shaking）  
🔲 虚拟滚动  
🔲 Web Worker  
🔲 图片懒加载  
🔲 性能监控  

### 7.3 预期效果

实施上述优化后，预期可以：
- **首屏加载时间**：减少 30-50%
- **内存占用**：减少 40-60%
- **交互响应速度**：提升 50-70%
- **滚动流畅度**：提升 60-80%

---

## 八、参考资料

- [Web.dev 性能优化指南](https://web.dev/performance/)
- [Vite 性能优化](https://vitejs.dev/guide/performance.html)
- [Vue 性能优化](https://vuejs.org/guide/best-practices/performance.html)
- [Chrome DevTools 性能分析](https://developer.chrome.com/docs/devtools/performance/)




