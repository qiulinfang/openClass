# 安卓原生加入课堂系统技术文档

## 目录
1. [系统概述](#系统概述)
2. [系统架构](#系统架构)
3. [核心组件](#核心组件)
4. [通信协议](#通信协议)
5. [API接口](#api接口)
6. [数据流程](#数据流程)
7. [配置参数](#配置参数)
8. [故障排除](#故障排除)
9. [开发指南](#开发指南)

---

## 系统概述

### 1.1 功能描述
安卓原生加入课堂系统是一个基于UDP组播通信的实时屏幕共享解决方案，支持多学生同时加入课堂，实现教师端对学生端的屏幕投屏控制和截图功能。

### 1.2 主要特性
- **实时屏幕共享**: 基于H264编码的高质量屏幕传输
- **多用户支持**: 支持多学生同时加入课堂
- **双向通信**: 教师端可控制学生端投屏和截图
- **状态同步**: 实时状态上报和同步机制
- **容错处理**: 完善的网络错误处理和重连机制
- **双模式支持**: 支持正式用户和游客模式

### 1.3 技术栈
- **开发语言**: Java (Android)
- **网络协议**: UDP组播 + TCP单播
- **视频编码**: H264
- **流媒体**: MPEG-TS
- **媒体处理**: FFmpeg
- **屏幕录制**: MediaProjection API

---

## 系统架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                    安卓客户端架构                              │
├─────────────────────────────────────────────────────────────┤
│  UI层                                                       │
│  ┌─────────────────┐  ┌─────────────────┐                  │
│  │ MyProfileView   │  │ 其他UI组件       │                  │
│  └─────────────────┘  └─────────────────┘                  │
├─────────────────────────────────────────────────────────────┤
│  业务逻辑层                                                  │
│  ┌─────────────────┐  ┌─────────────────┐                  │
│  │ ScreenCasting   │  │ ScreenShareKit  │                  │
│  │ Manager         │  │                 │                  │
│  └─────────────────┘  └─────────────────┘                  │
├─────────────────────────────────────────────────────────────┤
│  通信层                                                     │
│  ┌─────────────────┐  ┌─────────────────┐                  │
│  │ ScreenCasting   │  │ UDP/TCP         │                  │
│  │ Communicator    │  │ 网络通信         │                  │
│  └─────────────────┘  └─────────────────┘                  │
├─────────────────────────────────────────────────────────────┤
│  媒体处理层                                                  │
│  ┌─────────────────┐  ┌─────────────────┐                  │
│  │ H264MpegTS      │  │ H264IFrameCache │                  │
│  │ StreamerManager │  │                 │                  │
│  └─────────────────┘  └─────────────────┘                  │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 组件关系图

```
MyProfileActivity
    ↓
ScreenCastingManager
    ↓
ScreenCastingCommunicator ←→ UDP组播网络
    ↓
ScreenShareKit
    ↓
H264MpegTSStreamerManager
    ↓
H264IFrameCache
```

---

## 核心组件

### 3.1 MyProfileActivity - 用户界面入口

#### 3.1.1 主要职责
- 提供用户交互界面
- 处理加入/退出课堂的用户操作
- 管理UI状态更新

#### 3.1.2 关键方法

```java
/**
 * 加入课堂按钮点击处理
 * 支持正式用户和游客模式
 */
private void joinClassroom() {
    if (user.getId().equals("guest000")) {
        // 游客模式处理
        handleGuestMode();
    } else {
        // 正式用户模式处理
        handleFormalUserMode();
    }
}

/**
 * 处理正式用户加入课堂逻辑
 */
private void handleFormalUserMode() {
    if (ScreenCastingManager.isHavingClass()) {
        // 已在课堂中，显示退出确认
        showExitConfirmationDialog();
    } else {
        // 开始加入课堂流程
        startJoinClassroomProcess();
    }
}
```

#### 3.1.3 状态管理
- `fakeClassMode`: 游客模式状态标识
- UI状态与课堂状态同步更新

### 3.2 ScreenCastingManager - 课堂模式管理器

#### 3.2.1 主要职责
- 管理课堂模式状态
- 控制UDP通信器生命周期
- 处理网络错误和重连
- 提供状态查询接口

#### 3.2.2 核心属性

```java
public class ScreenCastingManager {
    private static boolean isHavingClass = false;
    private static ScreenCastingCommunicator communicator;
    private static NetworkStateListener networkStateListener;
    private static CommandHandler commandHandler;
    private static Handler mainHandler;
    private static ExecutorService executor;
}
```

#### 3.2.3 关键方法

```java
/**
 * 设置课堂模式状态
 * @param classMode true-进入课堂模式, false-退出课堂模式
 */
public static void setClassMode(boolean classMode) {
    isHavingClass = classMode;
    if (classMode) {
        startLoop();
    } else {
        stopLoop();
    }
}

/**
 * 启动通信循环
 * 初始化UDP通信器和相关组件
 */
private static void startLoop() {
    if (communicator == null) {
        communicator = new ScreenCastingCommunicator();
        setupNetworkStateListener();
        setupCommandHandler();
        communicator.start();
    }
}
```

#### 3.2.4 网络状态监听

```java
private static void setupNetworkStateListener() {
    networkStateListener = new NetworkStateListener() {
        @Override
        public void onNetworkError(Exception e) {
            Log.e(TAG, "网络错误: " + e.getMessage());
            // 处理网络错误，可在此处实现重连逻辑
        }
        
        @Override
        public void onNetworkRecovered() {
            Log.i(TAG, "网络恢复");
            // 网络恢复后的处理逻辑
        }
    };
}
```

### 3.3 ScreenCastingCommunicator - UDP通信器

#### 3.3.1 主要职责
- 管理UDP组播通信
- 处理教师端命令
- 发送状态上报消息
- 管理网络连接状态

#### 3.3.2 网络配置

```java
public class ScreenCastingCommunicator {
    // 组播地址和端口
    private static final String MULTICAST_ADDRESS = "239.255.100.1";
    private static final int CONTROL_MULTICAST_PORT = 5000;
    private static final int TEACHER_PAD_CONTROL_PORT = 5986;
    private static final int STUDENT_CONTROL_PORT = 5987;
    
    // TS流端口基础值
    private static final int TS_STREAM_PORT_BASE = 10000;
    
    // 截图相关端口
    private static final int SNAPSHOT_TCP_PORT = 5002;
}
```

#### 3.3.3 消息类型定义

```java
// 消息类型
private static final String MSG_TYPE_STATUS = "status";
private static final String MSG_TYPE_PROJECTION_PAD = "projection_pad";
private static final String MSG_TYPE_SNAPSHOT_PAD = "snapshot_pad";
private static final String MSG_TYPE_PROJECTION_PC = "projection_pc";
private static final String MSG_TYPE_PICTURE = "picture";

// 角色定义
private static final String ROLE_STUDENT = "student";
private static final String ROLE_TEACHER = "teacher";
private static final String ROLE_PC = "pc";
private static final String RECEIVER_ALL = "all";

// 状态定义
private static final String STATUS_READY = "ready";
private static final String STATUS_STREAMING = "streaming";
```

#### 3.3.4 状态上报机制

```java
/**
 * 发送状态消息
 * 格式: status,student,all,学生ID,学生姓名,IP地址,端口,状态
 */
private void sendStatusMessage() throws IOException {
    String status = isStreaming ? STATUS_STREAMING : STATUS_READY;
    String localIp = getLocalIpAddress();
    String[] parts = localIp.split("\\.");
    this.tsStreamPort = TS_STREAM_PORT_BASE + Integer.parseInt(parts[3]);
    
    String message = String.format("%s,%s,%s,%s,%s,%s,%d,%s",
            MSG_TYPE_STATUS,
            ROLE_STUDENT,
            RECEIVER_ALL,
            studentId,
            studentName,
            localIp,
            tsStreamPort,
            status);
    
    // 发送到PC端和教师端
    sendToPCDevice(message);
    sendToTeacherPad(message);
}
```

#### 3.3.5 命令处理机制

```java
/**
 * 处理教师端投屏命令
 * 格式: projection_pad,teacher,all,教师IP,学生ID列表
 */
private void processProjectionCommand(String[] parts) {
    String teacherIp = parts[3];
    if (!teacherPadDeviceIp.equals(teacherIp)) {
        teacherPadDeviceIp = teacherIp;
    }
    
    // 检查是否包含本学生ID
    boolean shouldProject = false;
    for (int i = 4; i < parts.length; i++) {
        if (studentId.equals(parts[i])) {
            shouldProject = true;
            break;
        }
    }
    
    // 根据命令更新投屏状态
    if (shouldProject && !isStreaming) {
        startProjection();
    } else if (!shouldProject && isStreaming) {
        stopProjection();
    }
}
```

### 3.4 ScreenShareKit - 屏幕共享核心

#### 3.4.1 主要职责
- 屏幕录制和H264编码
- 媒体投影权限管理
- 音视频数据回调处理
- 错误处理和状态管理

#### 3.4.2 初始化配置

```java
public void init() {
    // 配置屏幕录制参数
    ScreenShareConfig config = new ScreenShareConfig.Builder()
            .setWidth(1920)                    // 分辨率宽度
            .setHeight(1080)                   // 分辨率高度
            .setFrameRate(30)                 // 帧率
            .setBitRate(8 * 1024 * 1024)      // 码率 8Mbps
            .setAudioSampleRate(44100)        // 音频采样率
            .setAudioChannels(2)              // 音频声道数
            .setVideoCodec("h264")            // 视频编码格式
            .build();
    
    // 设置回调
    setH264DataCallback(this::onH264DataReceived);
    setErrorCallback(this::onError);
    setStartCallback(this::onStart);
}
```

#### 3.4.3 数据回调处理

```java
/**
 * H264数据回调处理
 * @param data H264编码数据
 * @param isKeyFrame 是否为关键帧
 */
private void onH264DataReceived(byte[] data, boolean isKeyFrame) {
    if (h264ToTsStreamer != null) {
        h264ToTsStreamer.onH264DataReceived(data, isKeyFrame);
    }
    
    // 关键帧缓存用于截图功能
    if (isKeyFrame) {
        H264IFrameCache.getInstance().onH264Frame(data);
    }
}
```

### 3.5 H264MpegTSStreamerManager - 视频流管理

#### 3.5.1 主要职责
- H264到MPEG-TS格式转换
- 流媒体传输管理
- FFmpeg集成和配置

#### 3.5.2 核心实现

```java
public class H264MpegTSStreamerManager {
    private static FFmpegPipeStreamer instance = null;
    public static final int ENCODE_FRAME_RATE = 30;
    
    /**
     * 获取单例实例
     */
    public static synchronized FFmpegPipeStreamer getInstance() {
        if (instance == null) {
            instance = new FFmpegPipeStreamer(
                "127.0.0.1",                    // 本地地址
                UdpForwarderManager.STREAMING_LOCAL_PORT,  // 本地端口
                ENCODE_FRAME_RATE,              // 帧率
                null                            // 额外参数
            );
        }
        return instance;
    }
}
```

### 3.6 H264IFrameCache - I帧缓存管理

#### 3.6.1 主要职责
- 缓存H264关键帧数据
- 支持截图功能
- 提供I帧数据访问接口

#### 3.6.2 核心实现

```java
public class H264IFrameCache {
    private static H264IFrameCache instance;
    private byte[] latestIFrame;
    private final Object lock = new Object();
    
    /**
     * 更新I帧数据
     */
    public void onH264Frame(byte[] frameData) {
        synchronized (lock) {
            latestIFrame = frameData.clone();
        }
    }
    
    /**
     * 获取最新的I帧数据
     */
    public byte[] getLatestIFrame() {
        synchronized (lock) {
            return latestIFrame != null ? latestIFrame.clone() : null;
        }
    }
}
```

---

## 通信协议

### 4.1 网络拓扑

```
┌─────────────┐    UDP组播     ┌─────────────┐
│   教师端     │ ←────────────→ │   PC端      │
│  (平板)     │   239.255.100.1 │  (服务器)   │
└─────────────┘      :5000     └─────────────┘
       ↑                              ↑
       │ UDP单播                      │ UDP组播
       │ :5986                       │ :5000
       ↓                              ↓
┌─────────────┐    UDP组播     ┌─────────────┐
│   学生端1    │ ←────────────→ │   学生端2    │
│  (手机)     │   239.255.100.1 │  (手机)     │
└─────────────┘      :5000     └─────────────┘
```

### 4.2 消息格式规范

#### 4.2.1 基本格式
```
消息类型,发送者,接收者,参数1,参数2,参数3,...
```

#### 4.2.2 状态消息
```
status,student,all,学生ID,学生姓名,IP地址,端口,状态
```

**示例**:
```
status,student,all,stu001,张三,192.168.1.100,10000,ready
status,student,all,stu001,张三,192.168.1.100,10000,streaming
```

#### 4.2.3 投屏控制消息
```
projection_pad,teacher,all,教师IP,学生ID1,学生ID2,学生ID3,...
```

**示例**:
```
projection_pad,teacher,all,192.168.1.50,stu001,stu002
```

#### 4.2.4 截图命令消息
```
snapshot_pad,teacher,学生ID,命令ID
```

**示例**:
```
snapshot_pad,teacher,stu001,cmd_20231201_001
```

### 4.3 端口分配规则

#### 4.3.1 控制端口
- **组播控制端口**: 5000
- **教师端监听端口**: 5986
- **学生端监听端口**: 5987
- **截图TCP端口**: 5002

#### 4.3.2 TS流端口
```
TS流端口 = 10000 + IP地址最后一位
```

**示例**:
- IP: 192.168.1.100 → TS端口: 10100
- IP: 192.168.1.101 → TS端口: 10101

### 4.4 通信时序

#### 4.4.1 加入课堂时序
```
学生端                    教师端                    PC端
  │                        │                       │
  │─── 状态上报(ready) ────→│                       │
  │                        │                       │
  │                        │─── 投屏命令 ─────────→│
  │                        │                       │
  │←── 投屏命令 ────────────│                       │
  │                        │                       │
  │─── 状态上报(streaming) ─→│                       │
  │                        │                       │
```

#### 4.4.2 截图时序
```
学生端                    教师端
  │                        │
  │←── 截图命令 ────────────│
  │                        │
  │─── TCP连接 ────────────→│
  │                        │
  │─── I帧数据(分包) ──────→│
  │                        │
  │─── 关闭连接 ────────────→│
  │                        │
```

---

## API接口

### 5.1 ScreenCastingManager API

#### 5.1.1 状态查询
```java
/**
 * 检查是否在课堂模式
 * @return true-在课堂中, false-不在课堂中
 */
public static boolean isHavingClass()
```

#### 5.1.2 模式控制
```java
/**
 * 设置课堂模式
 * @param classMode true-进入课堂, false-退出课堂
 */
public static void setClassMode(boolean classMode)
```

#### 5.1.3 网络状态监听
```java
public interface NetworkStateListener {
    void onNetworkError(Exception e);
    void onNetworkRecovered();
}
```

### 5.2 ScreenCastingCommunicator API

#### 5.2.1 生命周期管理
```java
/**
 * 启动通信器
 */
public void start()

/**
 * 停止通信器
 */
public void stop()
```

#### 5.2.2 命令处理
```java
public interface CommandHandler {
    void onStartProjection();
    void onStopProjection();
    void onTakeSnapshot(String commandId);
}
```

### 5.3 ScreenShareKit API

#### 5.3.1 初始化
```java
/**
 * 初始化屏幕共享
 */
public void init()
```

#### 5.3.2 控制方法
```java
/**
 * 开始屏幕录制
 */
public void start()

/**
 * 停止屏幕录制
 */
public void stop()
```

#### 5.3.3 回调接口
```java
public interface H264DataCallback {
    void onH264Data(byte[] data, boolean isKeyFrame);
}

public interface ErrorCallback {
    void onError(Exception e);
}

public interface StartCallback {
    void onStart();
}
```

---

## 数据流程

### 6.1 屏幕录制流程

```
屏幕内容
    ↓
MediaProjection API
    ↓
H264编码器
    ↓
H264数据回调
    ↓
H264MpegTSStreamerManager
    ↓
MPEG-TS格式转换
    ↓
UDP网络传输
```

### 6.2 状态同步流程

```
学生端状态变化
    ↓
ScreenCastingCommunicator
    ↓
构建状态消息
    ↓
UDP组播发送
    ↓
教师端/PC端接收
    ↓
更新状态显示
```

### 6.3 投屏控制流程

```
教师端发送投屏命令
    ↓
UDP组播传输
    ↓
学生端接收命令
    ↓
解析命令参数
    ↓
检查学生ID匹配
    ↓
更新投屏状态
    ↓
启动/停止屏幕录制
    ↓
发送状态更新
```

### 6.4 截图流程

```
教师端发送截图命令
    ↓
UDP单播传输
    ↓
学生端接收命令
    ↓
获取最新I帧数据
    ↓
Base64编码
    ↓
TCP分包传输
    ↓
教师端接收图片数据
```

---

## 配置参数

### 7.1 屏幕录制参数

| 参数 | 值 | 说明 |
|------|----|----|
| 分辨率宽度 | 1920 | 屏幕录制宽度 |
| 分辨率高度 | 1080 | 屏幕录制高度 |
| 帧率 | 30 | 每秒帧数 |
| 码率 | 8Mbps | 视频码率 |
| 音频采样率 | 44100Hz | 音频采样频率 |
| 音频声道 | 2 | 立体声 |
| 视频编码 | H264 | 视频编码格式 |

### 7.2 网络参数

| 参数 | 值 | 说明 |
|------|----|----|
| 组播地址 | 239.255.100.1 | 控制通信组播地址 |
| 组播端口 | 5000 | 控制通信端口 |
| 教师端端口 | 5986 | 教师端监听端口 |
| 学生端端口 | 5987 | 学生端监听端口 |
| TS流端口基数 | 10000 | TS流端口计算基数 |
| 截图TCP端口 | 5002 | 截图数据传输端口 |

### 7.3 超时参数

| 参数 | 值 | 说明 |
|------|----|----|
| 状态上报间隔 | 1000ms | 状态消息发送间隔 |
| 网络超时 | 5000ms | 网络操作超时时间 |
| 重连间隔 | 3000ms | 网络重连间隔 |

---

## 故障排除

### 8.1 常见问题

#### 8.1.1 无法加入课堂
**症状**: 点击"加入课堂"按钮无响应

**可能原因**:
1. 网络连接问题
2. 组播地址不可达
3. 权限不足

**解决方案**:
1. 检查网络连接状态
2. 确认防火墙设置
3. 检查媒体投影权限

#### 8.1.2 屏幕录制失败
**症状**: 加入课堂后无法看到屏幕内容

**可能原因**:
1. MediaProjection权限未获取
2. H264编码器初始化失败
3. 屏幕录制服务被系统杀死

**解决方案**:
1. 重新申请媒体投影权限
2. 检查设备性能是否足够
3. 重启应用重新初始化

#### 8.1.3 网络通信中断
**症状**: 状态上报失败，无法接收教师端命令

**可能原因**:
1. UDP组播被防火墙阻止
2. 网络地址变化
3. 组播路由配置问题

**解决方案**:
1. 检查防火墙UDP端口设置
2. 重新获取本地IP地址
3. 检查路由器组播支持

### 8.2 调试方法

#### 8.2.1 日志调试
```java
// 启用详细日志
Log.d(TAG, "发送状态消息: " + message);
Log.d(TAG, "接收命令: " + command);
Log.d(TAG, "H264数据大小: " + data.length);
```

#### 8.2.2 网络抓包
```bash
# 使用tcpdump抓取UDP组播包
tcpdump -i any host 239.255.100.1

# 使用Wireshark分析网络流量
# 过滤条件: udp.port == 5000
```

#### 8.2.3 状态检查
```java
// 检查课堂模式状态
boolean inClass = ScreenCastingManager.isHavingClass();

// 检查网络连接状态
boolean networkAvailable = isNetworkAvailable();

// 检查屏幕录制状态
boolean recording = ScreenShareKit.INSTANCE.isRecording();
```

### 8.3 性能优化

#### 8.3.1 内存优化
- 及时释放H264数据缓存
- 使用对象池管理频繁创建的对象
- 避免内存泄漏

#### 8.3.2 网络优化
- 调整状态上报频率
- 使用压缩算法减少数据量
- 实现网络质量自适应

#### 8.3.3 CPU优化
- 使用硬件编码器
- 降低录制分辨率
- 优化编码参数

---

## 开发指南

### 9.1 环境要求

#### 9.1.1 开发环境
- Android Studio 4.0+
- Android SDK API 21+
- Java 8+

#### 9.1.2 运行环境
- Android 5.0+ (API 21+)
- 支持MediaProjection的设备
- 网络环境支持UDP组播

### 9.2 集成步骤

#### 9.2.1 添加依赖
```gradle
dependencies {
    implementation 'com.github.wseemann:FFmpegMediaMetadataRetriever:1.0.14'
    // 其他必要依赖
}
```

#### 9.2.2 权限配置
```xml
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
<uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
<uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
<uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
<uses-permission android:name="android.permission.RECORD_AUDIO" />
```

#### 9.2.3 代码集成
```java
// 1. 初始化ScreenCastingManager
ScreenCastingManager.setClassMode(true);

// 2. 设置回调处理
ScreenCastingManager.setCommandHandler(new CommandHandler() {
    @Override
    public void onStartProjection() {
        // 处理开始投屏
    }
    
    @Override
    public void onStopProjection() {
        // 处理停止投屏
    }
    
    @Override
    public void onTakeSnapshot(String commandId) {
        // 处理截图命令
    }
});

// 3. 初始化ScreenShareKit
ScreenShareKit.INSTANCE.init();
ScreenShareKit.INSTANCE.start();
```

### 9.3 自定义扩展

#### 9.3.1 添加新的消息类型
```java
// 在ScreenCastingCommunicator中添加新的消息类型
private static final String MSG_TYPE_CUSTOM = "custom";

// 实现消息处理逻辑
private void processCustomMessage(String[] parts) {
    // 自定义消息处理逻辑
}
```

#### 9.3.2 扩展回调接口
```java
public interface ExtendedCommandHandler extends CommandHandler {
    void onCustomCommand(String data);
}
```

### 9.4 测试指南

#### 9.4.1 单元测试
```java
@Test
public void testScreenCastingManager() {
    // 测试课堂模式切换
    ScreenCastingManager.setClassMode(true);
    assertTrue(ScreenCastingManager.isHavingClass());
    
    ScreenCastingManager.setClassMode(false);
    assertFalse(ScreenCastingManager.isHavingClass());
}
```

#### 9.4.2 集成测试
```java
@Test
public void testNetworkCommunication() {
    // 测试UDP组播通信
    ScreenCastingCommunicator communicator = new ScreenCastingCommunicator();
    communicator.start();
    
    // 验证状态上报
    // 验证命令接收
}
```

### 9.5 部署注意事项

#### 9.5.1 网络配置
- 确保网络支持UDP组播
- 配置防火墙规则
- 设置正确的路由

#### 9.5.2 性能监控
- 监控CPU和内存使用
- 监控网络带宽消耗
- 监控电池消耗

#### 9.5.3 错误处理
- 实现完善的错误日志
- 提供用户友好的错误提示
- 实现自动恢复机制

---

## 总结

本文档详细介绍了安卓原生加入课堂系统的技术架构、实现原理和使用方法。该系统通过UDP组播通信实现了多用户实时屏幕共享，具有良好的扩展性和稳定性。

### 主要特点
1. **模块化设计**: 各组件职责清晰，便于维护和扩展
2. **实时通信**: 基于UDP组播的高效通信机制
3. **高质量传输**: H264编码确保视频质量
4. **容错处理**: 完善的错误处理和重连机制
5. **易于集成**: 提供清晰的API接口

### 适用场景
- 在线教育平台
- 远程协作系统
- 屏幕共享应用
- 实时通信系统

通过本文档，开发者可以快速理解系统架构，进行二次开发和定制化改造。
